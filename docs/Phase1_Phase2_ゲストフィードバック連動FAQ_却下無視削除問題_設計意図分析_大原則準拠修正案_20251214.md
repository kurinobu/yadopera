# Phase 1・Phase 2: ゲストフィードバック連動FAQ 却下・無視削除問題 設計意図分析・大原則準拠修正案

**作成日**: 2025年12月14日  
**実施者**: AI Assistant  
**対象**: ゲストフィードバック連動FAQの却下・無視削除問題の設計意図分析と修正案  
**状態**: 📋 **設計意図分析・修正案提示完了（修正指示待ち）**

---

## 1. 大原則の確認

### 1.1 実装・修正の大原則（要約定義書より）

1. **根本解決 > 暫定解決**: 一時的な対処よりも根本的な解決を優先
2. **シンプル構造 > 複雑構造**: 複雑な実装よりもシンプルで理解しやすい構造を優先
3. **統一・同一化 > 特殊独自**: 特殊な実装よりも統一されたパターンを優先
4. **具体的 > 一般**: 抽象的な実装よりも具体的で明確な実装を優先
5. **拙速 < 安全確実**: 速度よりも安全性と確実性を優先
6. **Docker環境必須 > ローカル直接実行**: すべての修正・テストはDocker環境で実行する

---

## 2. ゲストフィードバックの設計意図の分析

### 2.1 ゲストフィードバックの存在意義と目的

#### 2.1.1 要約定義書からの設計意図

**ゲストフィードバック機能の目的**:
1. **AI回答の品質評価**: ゲストが回答に対して👍👎で評価を送信
2. **低評価回答の自動ハイライト**: 👎評価が2回以上ついた回答を自動ハイライト
3. **FAQ改善の促進**: 「FAQ改善提案」ボタンで回答文修正案を自動生成
4. **統計データの収集**: ゲストフィードバック集計（👍👎の比率、低評価回答リスト）
5. **データ保持**: `guest_feedback`は匿名化して統計データとして保持（GDPR準拠）

**設計意図**:
- ゲストフィードバックは**ゲストが実際に送信した評価データ**であり、削除すべきではない
- 低評価回答は**改善の機会**として扱い、FAQ改善提案を生成する
- 統計データとして保持し、分析に活用する

#### 2.1.2 アーキテクチャ設計書からの設計意図

**ゲストフィードバック連動機能**:
- 低評価回答の自動ハイライト（v0.3新規）
- FAQ改善提案の自動生成（v0.3新規）
- UI設計: `[FAQ改善提案] [無視]`ボタン

**設計意図**:
- 低評価回答に対して、管理者が「FAQ改善提案」または「無視」を選択できる
- 「FAQ改善提案」: 回答文修正案を自動生成し、FAQ改善を促進
- 「無視」: ノイズ質問や改善不要な回答を除外

### 2.2 「却下」と「無視」の設計意図の分析

#### 2.2.1 「却下」の設計意図

**現在の実装**:
- 「却下」は**FAQ提案を却下する**機能
- FAQ提案（`faq_suggestions`）の`status`を`"rejected"`に更新

**設計意図**:
- FAQ提案は「改善案」であり、管理者が承認・却下を判断する
- 「却下」は「この改善案は採用しない」という意思表示
- **低評価回答自体は削除されない**（ゲストが実際に送信した評価データのため）

#### 2.2.2 「無視」の設計意図

**要約定義書からの記載**:
- 「無視」ボタン（ノイズ質問除外）: 未解決質問リストの「無視」ボタン
- ゲストフィードバック連動: `[FAQ改善提案] [無視]`ボタン

**設計意図**:
- 「無視」は**低評価回答を処理済みとして扱い、画面から非表示にする**機能
- ノイズ質問や改善不要な回答を除外する
- **低評価フィードバック自体は削除されない**（統計データとして保持）

### 2.3 設計意図のまとめ

**ゲストフィードバックの設計意図**:
1. **ゲストが実際に送信した評価データ**: 削除すべきではない（統計データとして重要）
2. **改善の機会**: 低評価回答はFAQ改善の機会として扱う
3. **処理済みの管理**: 「無視」機能で処理済みとして扱い、画面から非表示にする

**「却下」と「無視」の設計意図**:
1. **「却下」**: FAQ提案を却下する（改善案を採用しない）
2. **「無視」**: 低評価回答を処理済みとして扱い、画面から非表示にする

---

## 3. 現在の実装と設計意図のギャップ

### 3.1 設計意図と実装の不一致

**設計意図**:
- 「無視」: 低評価回答を処理済みとして扱い、画面から非表示にする

**現在の実装**:
- 「無視」: 実装されていない（TODOコメントのみ）
- 「却下」: FAQ提案を却下するだけで、低評価回答は非表示にならない

**ギャップ**: 
- 設計意図では「無視」機能が想定されているが、実装されていない
- 「却下」はFAQ提案を却下する機能であり、低評価回答を非表示にする機能ではない

### 3.2 ユーザーの期待と設計意図の整合性

**ユーザーの期待**:
- 「却下」や「無視」をクリックしたら、その低評価回答が画面から削除される

**設計意図**:
- 「無視」: 低評価回答を処理済みとして扱い、画面から非表示にする（設計意図と一致）
- 「却下」: FAQ提案を却下する機能（低評価回答を非表示にする機能ではない）

**整合性**:
- 「無視」機能の実装により、ユーザーの期待と設計意図が一致する
- 「却下」はFAQ提案を却下する機能として明確に分離する

---

## 4. 「却下」と「無視」の動作と表示の設計

### 4.1 「却下」の動作と表示

#### 4.1.1 設計意図に基づく動作

**「却下」の意味**:
- **FAQ提案を却下する**（この改善案は採用しない）

**動作**:
1. FAQ提案の`status`を`"rejected"`に更新
2. FAQ提案編集画面を閉じる
3. **低評価回答は画面に残る**（FAQ提案を却下しても、低評価回答自体は削除されない）

**表示**:
- 「却下」ボタンは**FAQ提案編集画面内**に表示される
- 低評価回答リストには表示されない（低評価回答リストは「FAQ改善提案」ボタンのみ）

#### 4.1.2 ユーザー体験の改善

**現在の問題**:
- ユーザーが「却下」をクリックしたら、低評価回答が画面から削除されることを期待している

**設計意図に基づく改善**:
- 「却下」はFAQ提案を却下する機能であることを明確にする
- 低評価回答を非表示にしたい場合は「無視」ボタンを使用することを案内

### 4.2 「無視」の動作と表示

#### 4.2.1 設計意図に基づく動作

**「無視」の意味**:
- **低評価回答を処理済みとして扱い、画面から非表示にする**

**動作**:
1. 低評価回答を「無視」状態として記録
2. 低評価回答リストから除外
3. 画面から非表示にする

**表示**:
- 「無視」ボタンは**低評価回答リスト内**に表示される
- 「FAQ改善提案」ボタンと並んで表示される

---

## 5. 大原則準拠修正案

### 5.1 修正案A: 低評価回答を「無視」する機能を実装（推奨）

#### 5.1.1 設計意図に基づく実装

**内容**:
1. **データベーススキーマの変更**: 無視された低評価回答を記録する仕組みを追加
2. **バックエンドAPIエンドポイントの追加**: `POST /admin/feedback/{message_id}/ignore`
3. **`get_negative_feedbacks`関数の修正**: 無視された低評価回答を除外
4. **フロントエンドの実装**: 「無視」ボタンの処理を実装

**大原則準拠評価**:
- ✅ **根本解決**: 低評価回答を「処理済み」として扱う仕組みを実装することで、根本的に解決
- ✅ **シンプル構造**: 新しいテーブル`ignored_feedbacks`を作成するシンプルな実装
- ✅ **統一・同一化**: 既存のAPIエンドポイントのパターンに従う
- ✅ **具体的**: 明確な実装方法（データベーススキーマ、APIエンドポイント、フロントエンド）
- ✅ **安全確実**: ゲストフィードバックは削除せず、統計データとして保持

**準拠度**: 100%

#### 5.1.2 データベーススキーマの設計

**オプション1: 新しいテーブル`ignored_feedbacks`を作成（推奨）**

**設計理由**:
- ゲストフィードバック（`guest_feedback`）はゲストが送信した評価データ
- 「無視」は管理者の操作であり、別テーブルで管理するのが設計上適切
- ゲストフィードバックテーブルを汚染しない

**マイグレーション**:
```python
# alembic/versions/XXX_create_ignored_feedbacks_table.py
def upgrade():
    op.create_table(
        'ignored_feedbacks',
        sa.Column('id', sa.Integer(), primary_key=True),
        sa.Column('message_id', sa.Integer(), sa.ForeignKey('messages.id', ondelete='CASCADE'), nullable=False),
        sa.Column('facility_id', sa.Integer(), sa.ForeignKey('facilities.id', ondelete='CASCADE'), nullable=False),
        sa.Column('ignored_at', sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False),
        sa.Column('ignored_by', sa.Integer(), sa.ForeignKey('users.id', ondelete='SET NULL'), nullable=True),
        sa.UniqueConstraint('message_id', 'facility_id', name='uq_ignored_feedback_message_facility')
    )
    op.create_index('idx_ignored_feedbacks_message_id', 'ignored_feedbacks', ['message_id'])
    op.create_index('idx_ignored_feedbacks_facility_id', 'ignored_feedbacks', ['facility_id'])
```

**メリット**:
- 設計上適切（管理者の操作を別テーブルで管理）
- ゲストフィードバックテーブルを汚染しない
- 統計データとして保持できる

#### 5.1.3 バックエンドAPIエンドポイントの実装

**ファイル**: `backend/app/api/v1/admin/feedback.py`

**追加内容**:
```python
@router.post("/{message_id}/ignore")
async def ignore_negative_feedback(
    message_id: int,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """
    低評価回答を無視
    
    JWT認証必須。指定されたメッセージIDの低評価回答を無視します。
    無視された低評価回答は、低評価回答リストから除外されます。
    """
    try:
        # ユーザーが所属する施設IDを取得
        facility_id = current_user.facility_id
        if not facility_id:
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail="User is not associated with any facility"
            )
        
        # フィードバックサービスで低評価回答を無視
        feedback_service = FeedbackService(db)
        await feedback_service.ignore_negative_feedback(
            message_id=message_id,
            facility_id=facility_id,
            user_id=current_user.id
        )
        
        return {"message": "Negative feedback ignored successfully"}
    
    except ValueError as e:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail=str(e)
        )
    except HTTPException:
        raise
    except Exception as e:
        raise HTTPException(
            status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
            detail=f"Error ignoring negative feedback: {str(e)}"
        )
```

#### 5.1.4 バックエンドサービスの実装

**ファイル**: `backend/app/services/feedback_service.py`

**追加内容**:
```python
async def ignore_negative_feedback(
    self,
    message_id: int,
    facility_id: int,
    user_id: int
) -> None:
    """
    低評価回答を無視
    
    Args:
        message_id: メッセージID
        facility_id: 施設ID
        user_id: 無視したユーザーID
    
    Raises:
        ValueError: メッセージが見つからない場合、または既に無視されている場合
    """
    # メッセージを取得
    message = await self.db.get(Message, message_id)
    if not message:
        raise ValueError(f"Message not found: message_id={message_id}")
    
    # 会話を取得して施設IDを確認
    conversation = await self.db.get(Conversation, message.conversation_id)
    if not conversation:
        raise ValueError(f"Conversation not found: conversation_id={message.conversation_id}")
    
    if conversation.facility_id != facility_id:
        raise ValueError(f"Message does not belong to facility: message_id={message_id}, facility_id={facility_id}")
    
    # 既に無視されているか確認
    existing_result = await self.db.execute(
        select(IgnoredFeedback).where(
            IgnoredFeedback.message_id == message_id,
            IgnoredFeedback.facility_id == facility_id
        )
    )
    existing = existing_result.scalar_one_or_none()
    if existing:
        raise ValueError(f"Negative feedback already ignored: message_id={message_id}")
    
    # 無視状態を記録
    ignored_feedback = IgnoredFeedback(
        message_id=message_id,
        facility_id=facility_id,
        ignored_by=user_id
    )
    self.db.add(ignored_feedback)
    await self.db.commit()
    
    logger.info(
        f"Negative feedback ignored: message_id={message_id}, facility_id={facility_id}, user_id={user_id}",
        extra={
            "message_id": message_id,
            "facility_id": facility_id,
            "user_id": user_id
        }
    )
```

#### 5.1.5 `get_negative_feedbacks`関数の修正

**修正内容**:
- 無視された低評価回答を除外する処理を追加

**修正後**:
```python
async def get_negative_feedbacks(
    self,
    facility_id: int
) -> List[LowRatedAnswer]:
    """
    低評価回答リスト取得（2回以上低評価がついた回答）
    無視された低評価回答は除外されます。
    
    Args:
        facility_id: 施設ID
    
    Returns:
        List[LowRatedAnswer]: 低評価回答リスト
    """
    # フィードバックを取得
    feedback_result = await self.db.execute(
        select(GuestFeedback)
        .where(
            GuestFeedback.facility_id == facility_id,
            GuestFeedback.feedback_type == "negative"
        )
    )
    feedbacks = feedback_result.scalars().all()
    
    # メッセージIDごとに低評価数を集計
    message_negative_count: dict[int, int] = {}
    for feedback in feedbacks:
        message_negative_count[feedback.message_id] = message_negative_count.get(feedback.message_id, 0) + 1
    
    # 2回以上低評価がついたメッセージIDを取得
    low_rated_message_ids = [msg_id for msg_id, count in message_negative_count.items() if count >= 2]
    
    if not low_rated_message_ids:
        return []
    
    # 無視されたメッセージIDを取得
    ignored_result = await self.db.execute(
        select(IgnoredFeedback.message_id).where(
            IgnoredFeedback.facility_id == facility_id,
            IgnoredFeedback.message_id.in_(low_rated_message_ids)
        )
    )
    ignored_message_ids = set(ignored_result.scalars().all())
    
    # 無視されたメッセージIDを除外
    low_rated_message_ids = [msg_id for msg_id in low_rated_message_ids if msg_id not in ignored_message_ids]
    
    if not low_rated_message_ids:
        return []
    
    # メッセージを取得（会話も一緒に取得）
    messages_result = await self.db.execute(
        select(Message)
        .where(Message.id.in_(low_rated_message_ids))
        .options(selectinload(Message.conversation))
    )
    messages = messages_result.scalars().all()
    
    # ... (既存の処理)
```

#### 5.1.6 フロントエンドの実装

**ファイル**: `frontend/src/api/feedback.ts`

**追加内容**:
```typescript
export const feedbackApi = {
  // ... (既存のメソッド)
  
  /**
   * 低評価回答を無視
   */
  async ignoreNegativeFeedback(messageId: number): Promise<void> {
    await apiClient.post(`/admin/feedback/${messageId}/ignore`)
  }
}
```

**ファイル**: `frontend/src/views/admin/FaqManagement.vue`

**修正内容**:
```typescript
const handleFeedbackIgnore = async (answer: LowRatedAnswer) => {
  if (!confirm('この低評価回答を無視しますか？無視した回答は画面から非表示になります。')) {
    return
  }
  
  try {
    await feedbackApi.ignoreNegativeFeedback(answer.message_id)
    // 低評価回答リストを再取得（画面に反映）
    await fetchLowRatedAnswers()
  } catch (err: any) {
    console.error('Failed to ignore negative feedback:', err)
    alert(err.response?.data?.detail || '低評価回答の無視に失敗しました')
  }
}
```

### 5.2 修正案B: 「却下」ボタンの動作の明確化（オプション）

#### 5.2.1 設計意図に基づく動作の明確化

**内容**:
- 「却下」はFAQ提案を却下する機能であることを明確にする
- 低評価回答を非表示にしたい場合は「無視」ボタンを使用することを案内

**実装方法**:
- 「却下」ボタンのツールチップや説明文を追加
- または、「却下」ボタンをクリックした際に、低評価回答を非表示にするか確認する

**大原則準拠評価**:
- ✅ **根本解決**: ユーザーの期待と設計意図のギャップを明確にする
- ✅ **シンプル構造**: UIの改善のみで、実装がシンプル
- ✅ **統一・同一化**: 既存のUIパターンに従う
- ✅ **具体的**: 明確な実装方法
- ✅ **安全確実**: 既存の動作を維持し、説明を追加するだけ

**準拠度**: 100%

**結論**: 修正案Aを優先して実施。修正案Bはオプション（UI改善）

---

## 6. 修正内容の詳細

### 6.1 データベーススキーマの変更

#### 6.1.1 新しいテーブル`ignored_feedbacks`の作成

**ファイル**: `backend/app/models/ignored_feedback.py`（新規作成）

**内容**:
```python
"""
無視された低評価回答モデル
"""
from sqlalchemy import Column, Integer, DateTime, ForeignKey, UniqueConstraint
from sqlalchemy.orm import relationship
from sqlalchemy.sql import func
from app.database import Base


class IgnoredFeedback(Base):
    """
    無視された低評価回答モデル
    管理者が無視した低評価回答を記録
    """
    __tablename__ = "ignored_feedbacks"

    id = Column(Integer, primary_key=True, index=True)
    message_id = Column(Integer, ForeignKey("messages.id", ondelete="CASCADE"), nullable=False, index=True)
    facility_id = Column(Integer, ForeignKey("facilities.id", ondelete="CASCADE"), nullable=False, index=True)
    ignored_at = Column(DateTime(timezone=True), server_default=func.now(), nullable=False)
    ignored_by = Column(Integer, ForeignKey("users.id", ondelete="SET NULL"), nullable=True)

    # リレーションシップ
    message = relationship("Message")
    facility = relationship("Facility")
    ignored_by_user = relationship("User", foreign_keys=[ignored_by])

    __table_args__ = (
        UniqueConstraint('message_id', 'facility_id', name='uq_ignored_feedback_message_facility'),
    )
```

#### 6.1.2 Alembicマイグレーションの作成

**ファイル**: `backend/alembic/versions/XXX_create_ignored_feedbacks_table.py`（新規作成）

**内容**:
```python
"""create ignored_feedbacks table

Revision ID: xxxxx
Revises: yyyyy
Create Date: 2025-12-14

"""
from alembic import op
import sqlalchemy as sa


def upgrade():
    op.create_table(
        'ignored_feedbacks',
        sa.Column('id', sa.Integer(), nullable=False),
        sa.Column('message_id', sa.Integer(), nullable=False),
        sa.Column('facility_id', sa.Integer(), nullable=False),
        sa.Column('ignored_at', sa.DateTime(timezone=True), server_default=sa.func.now(), nullable=False),
        sa.Column('ignored_by', sa.Integer(), nullable=True),
        sa.ForeignKeyConstraint(['message_id'], ['messages.id'], ondelete='CASCADE'),
        sa.ForeignKeyConstraint(['facility_id'], ['facilities.id'], ondelete='CASCADE'),
        sa.ForeignKeyConstraint(['ignored_by'], ['users.id'], ondelete='SET NULL'),
        sa.PrimaryKeyConstraint('id'),
        sa.UniqueConstraint('message_id', 'facility_id', name='uq_ignored_feedback_message_facility')
    )
    op.create_index('idx_ignored_feedbacks_message_id', 'ignored_feedbacks', ['message_id'])
    op.create_index('idx_ignored_feedbacks_facility_id', 'ignored_feedbacks', ['facility_id'])


def downgrade():
    op.drop_table('ignored_feedbacks')
```

### 6.2 バックエンドの実装

#### 6.2.1 モデルの追加

**ファイル**: `backend/app/models/__init__.py`

**追加内容**:
```python
from app.models.ignored_feedback import IgnoredFeedback
```

#### 6.2.2 サービスの実装

**ファイル**: `backend/app/services/feedback_service.py`

**追加内容**:
- `ignore_negative_feedback`メソッドの実装
- `get_negative_feedbacks`メソッドの修正（無視された低評価回答を除外）

#### 6.2.3 APIエンドポイントの追加

**ファイル**: `backend/app/api/v1/admin/feedback.py`

**追加内容**:
- `POST /admin/feedback/{message_id}/ignore`エンドポイントの実装

### 6.3 フロントエンドの実装

#### 6.3.1 APIクライアントの追加

**ファイル**: `frontend/src/api/feedback.ts`

**追加内容**:
- `ignoreNegativeFeedback`メソッドの実装

#### 6.3.2 コンポーネントの修正

**ファイル**: `frontend/src/views/admin/FaqManagement.vue`

**修正内容**:
- `handleFeedbackIgnore`関数の実装

---

## 7. テスト計画

### 7.1 単体テスト

**テストケース1: 「無視」ボタンの動作確認**
1. 低評価回答を選択
2. 「無視」ボタンをクリック
3. 確認モーダルが表示されることを確認
4. OKをクリック
5. **期待結果**: 低評価回答が画面から削除される

**テストケース2: 無視された低評価回答の除外確認**
1. 低評価回答を無視
2. 低評価回答リストを再取得
3. **期待結果**: 無視された低評価回答がリストに含まれない

**テストケース3: 既に無視されている低評価回答の再無視**
1. 既に無視されている低評価回答に対して「無視」ボタンをクリック
2. **期待結果**: エラーメッセージが表示される

### 7.2 統合テスト

**テストケース1: 「無視」ボタンの統合テスト**
1. 管理画面にログイン
2. 「ゲストフィードバック連動FAQ」ページを開く
3. 低評価回答を選択して「無視」ボタンをクリック
4. 確認モーダルでOKをクリック
5. 低評価回答が画面から削除されることを確認

**テストケース2: 「却下」ボタンの動作確認**
1. 低評価回答を選択して「FAQ改善提案」ボタンをクリック
2. FAQ提案編集画面が表示されることを確認
3. 「却下」ボタンをクリック
4. **期待結果**: FAQ提案が却下され、FAQ提案編集画面が閉じる。低評価回答は画面に残る（設計意図通り）

---

## 8. 実装手順

### 8.1 ステップ1: データベーススキーマの変更

1. `backend/app/models/ignored_feedback.py`を作成
2. Alembicマイグレーションファイルを作成
3. マイグレーションを実行
4. データベースの状態を確認

### 8.2 ステップ2: バックエンドの実装

1. `backend/app/services/feedback_service.py`に`ignore_negative_feedback`メソッドを追加
2. `get_negative_feedbacks`メソッドを修正（無視された低評価回答を除外）
3. `backend/app/api/v1/admin/feedback.py`にAPIエンドポイントを追加
4. バックエンドを再起動して動作確認

### 8.3 ステップ3: フロントエンドの実装

1. `frontend/src/api/feedback.ts`に`ignoreNegativeFeedback`メソッドを追加
2. `frontend/src/views/admin/FaqManagement.vue`の`handleFeedbackIgnore`関数を実装
3. 動作確認

### 8.4 ステップ4: 動作確認

1. Docker環境で「無視」ボタンをテスト
2. 低評価回答が画面から削除されることを確認
3. 無視された低評価回答がリストに含まれないことを確認

---

## 9. リスク分析

### 9.1 リスク1: データベーススキーマの変更による影響

**リスク**: マイグレーション実行時に既存データへの影響

**影響**: 
- 新しいテーブルを作成するだけなので、既存データへの影響はない

**対策**:
- マイグレーションを実行前にバックアップを取る
- テスト環境でマイグレーションを実行して確認

**リスクレベル**: 低

### 9.2 リスク2: 既存の動作への影響

**リスク**: `get_negative_feedbacks`関数の修正により、既存の動作に影響する可能性がある

**影響**: 
- 無視された低評価回答がリストに含まれなくなる（期待される動作）

**対策**:
- 十分なテストを実施して、既存の動作に影響がないことを確認
- 無視された低評価回答が正しく除外されることを確認

**リスクレベル**: 低

### 9.3 リスク3: パフォーマンスへの影響

**リスク**: `get_negative_feedbacks`関数で無視された低評価回答を除外する処理により、パフォーマンスが低下する可能性がある

**影響**: 
- 追加のクエリが必要になるが、インデックスを使用するため影響は限定的

**対策**:
- インデックスを適切に設定する
- パフォーマンステストを実施する

**リスクレベル**: 低

---

## 10. 大原則準拠の総合評価

### 10.1 根本解決 > 暫定解決

**評価**: ✅ **完全準拠**

**理由**:
- 低評価回答を「処理済み」として扱う仕組みを実装することで、根本的に解決
- ゲストフィードバックは削除せず、統計データとして保持（設計意図に準拠）

**準拠度**: 100%

### 10.2 シンプル構造 > 複雑構造

**評価**: ✅ **完全準拠**

**理由**:
- 新しいテーブル`ignored_feedbacks`を作成するシンプルな実装
- 既存のAPIエンドポイントのパターンに従う
- 既存のコードを最小限の修正で拡張

**準拠度**: 100%

### 10.3 統一・同一化 > 特殊独自

**評価**: ✅ **完全準拠**

**理由**:
- 既存のAPIエンドポイントのパターンに従う（`POST /admin/feedback/{message_id}/ignore`）
- 既存のサービスメソッドのパターンに従う
- 既存のフロントエンドのパターンに従う

**準拠度**: 100%

### 10.4 具体的 > 一般

**評価**: ✅ **完全準拠**

**理由**:
- 明確な実装方法（データベーススキーマ、APIエンドポイント、フロントエンド）
- 具体的なコード例を提示
- 実行可能な具体的な内容を記載

**準拠度**: 100%

### 10.5 拙速 < 安全確実

**評価**: ✅ **完全準拠**

**理由**:
- 十分なテスト計画を提示
- リスク分析を実施
- 既存の動作に影響がないことを確認するテストを実施
- マイグレーション実行前にバックアップを取る

**準拠度**: 100%

### 10.6 総合評価

**平均準拠度**: 100%

**結論**: ✅ **大原則に完全準拠した修正案**

---

## 11. まとめ

### 11.1 ゲストフィードバックの設計意図

1. **ゲストが実際に送信した評価データ**: 削除すべきではない（統計データとして重要）
2. **改善の機会**: 低評価回答はFAQ改善の機会として扱う
3. **処理済みの管理**: 「無視」機能で処理済みとして扱い、画面から非表示にする

### 11.2 「却下」と「無視」の設計意図

1. **「却下」**: FAQ提案を却下する（改善案を採用しない）
2. **「無視」**: 低評価回答を処理済みとして扱い、画面から非表示にする

### 11.3 修正内容

1. **「無視」機能の実装**: 低評価回答を無視する機能を実装（データベーススキーマの変更、APIエンドポイントの追加、フロントエンドの実装）
2. **`get_negative_feedbacks`関数の修正**: 無視された低評価回答を除外する処理を追加
3. **「却下」ボタンの動作の明確化**: FAQ提案を却下する機能であることを明確にする（オプション）

### 11.4 期待される効果

1. **ユーザーの期待に沿った動作**: 「無視」ボタンをクリックしたら、低評価回答が画面から削除される
2. **設計意図に準拠**: ゲストフィードバックは削除せず、統計データとして保持
3. **処理済みの管理**: 低評価回答を「処理済み」として管理できる

---

**修正案提示完了日**: 2025年12月14日  
**状態**: 📋 **修正指示待ち**


