# サービス開始までの手順 Runbook

**作成日**: 2026年3月19日  
**最終更新日**: 2026年3月21日  
**目的**: 本番リリース当日に、手順漏れなく安全にサービス開始するための実行手順書。  
**対象環境**: `main`（本番） / Render（Backend + Frontend） / 本番DB / Redis / Stripe本番。  
**前提**: すべての修正・検証は Docker 環境で完了済みであること。

**LP・公式サイト（`landing/`）本番**: `docs/サービス開始までの手順整理_実装計画.md` **§6.2 順9** まで**保留**（品質ゲート・SaaS 本番 Go 後）。本 Runbook は主に **Render SaaS 本番** と **Stripe 本番切替**向け。

**補足（2026-03-21）**: 順9 前であっても **ティザー文言のみ**本番で直す場合は **`main` の `landing/index.html` を更新して push** すれば GitHub Pages が走る（品質ゲートの「本番LP全面公開」とは別の緊急・軽微更新として扱う）。**`develop` のみでは `yadopera.com` は変わらない**。記録: `docs/20260321_LPティザー_GitHubPages_ブランチとデプロイ不手際_記録.md`。

---

## 0. 役割分担

- **実行担当（Dev）**: 手順実行、ログ収集、一次判定。
- **確認担当（Reviewer）**: 各フェーズのチェック項目を読み合わせし承認。
- **最終承認者（Owner）**: Go/No-Go判定、公開可否決定。

---

## 1. T-14日〜T-1日 事前準備

### 1.1 コード凍結前確認（T-14日〜T-7日）

- [ ] 残存課題のうち「本番開始を阻害する項目」を棚卸しし、対応方針を確定。
- [ ] `develop` の最新動作を Docker で再確認（API/管理画面/ゲスト導線）。
- [ ] `feature/* -> develop` マージを完了し、未マージ差分をゼロにする。
- [ ] 旧運用メモとの不整合（旧Vercel記述など）を本Runbookに統一。

### 1.2 本番設定準備（T-7日〜T-3日）

- [ ] Render本番 Backend サービス設定（ブランチ `main`）を確認。
- [ ] Render本番 Frontend サービス設定（ブランチ `main`）を確認。
- [ ] 本番 `DATABASE_URL` / `REDIS_URL` / `SECRET_KEY` / `CORS_ORIGINS` を確認。
- [ ] Stripe本番キーと Price ID、Webhook Secret を確認（誤って test を混在させない）。
- [ ] ログ・監視確認手段（Render Logs / Stripe event logs）を準備。

### 1.3 データ保全準備（T-2日〜T-1日）

- [ ] 本番DBバックアップ手順を確定（管理コンソールまたは `pg_dump`）。
- [ ] ロールバック時に戻す対象（アプリ/DB/環境変数）を明文化。
- [ ] 障害連絡チャネルと判断者を固定（Ownerに即時連絡可能な状態）。

### 1.4 品質ゲート（T-7日以前推奨・LP本番禁止の解除条件）

実装計画 **§6.2 順2** 相当。GitHub Pages 本番を出す前に必ず完了する。

- [ ] **Docker** で API / 管理画面 / ゲスト導線の回帰（証跡：日付・実施者）。
- [ ] **ステージング**で Stripe **test mode** の必須シナリオ完走（実装計画 **§6.5** と同一項目でチェック）。
- [ ] 本番ブロッカー Issue がゼロ、または Owner 承認の許容例外として文書化。

### 1.5 Stripe 本番キー切替**前**の再確認（T-3日〜T-1日）

実装計画 **§6.4** の「テストモード」行に準拠。ステージングで一度通っていても **本番直前に再実行**推奨。

- [ ] 本番用 Price ID / Webhook エンドポイント（本番 API URL）/ 署名シークレットの**読み合わせ**（test と live の混在なし）。
- [ ] `docs/領収証_インボイス対応化_設定計画.md` と表示・番号体系が整合。

---

## 2. 当日手順（T=0）

### 2.1 リリース開始宣言

- [ ] 作業開始時刻を記録。
- [ ] `develop` と `main` の最終差分確認（意図しない差分がないこと）。
- [ ] 作業中は `main` への直接コミット禁止、緊急時以外は手順外作業を行わない。

### 2.2 事前バックアップ

- [ ] 本番DBバックアップを取得。
- [ ] バックアップ取得時刻と保管先を記録。
- [ ] バックアップ復元手順の確認（最低1回、机上確認）。

### 2.3 デプロイ実施

- [ ] `develop` で最終確認済みコミットを `main` に反映。
- [ ] `main` push 後、Render 自動デプロイ開始を確認。
- [ ] Backend/Frontend のデプロイ完了ステータスを確認。

### 2.4 DBマイグレーション確認

- [ ] 本番環境で `alembic upgrade head` が完了していることを確認。
- [ ] `alembic_version` が期待リビジョンであることを確認。
- [ ] 主要テーブルの存在確認（facilities/messages/faqs/stripe関連カラム）。

---

## 3. リリース直後の機能確認（30分以内）

### 3.1 基本ヘルス

- [ ] `GET /api/v1/health` が 200。
- [ ] ルート応答が 200（Backend）。
- [ ] Frontendのトップ表示が正常。

### 3.2 主要導線（管理者）

- [ ] ログイン -> ダッシュボード表示。
- [ ] FAQ一覧表示。
- [ ] プラン・請求ページ表示。
- [ ] 請求履歴・領収書導線表示。

### 3.3 主要導線（ゲスト）

- [ ] 施設URLアクセス。
- [ ] 会話開始・メッセージ送信。
- [ ] FAQベース応答・履歴表示。

### 3.4 決済・Webhook（本番キー切替直後）

実装計画 **§6.4「本番キー切替直後」**・**§6.5** のシナリオに沿って確認する。

- [ ] Stripe Webhook の受信ログにエラー多発がない（署名検証失敗が続いていない）。
- [ ] **新規登録〜有料プラン**（または合意したテスト施設のみの実決済）で Customer / Subscription / 管理画面表示が一致。
- [ ] プラン変更・解約でサブスク状態と DB / 画面が整合。
- [ ] 請求履歴・領収書（・インボイス要件）が致命障害なく開ける。
- [ ] 失敗イベントをログで追跡可能（必要なら Stripe ダッシュボードで再送確認）。

---

## 4. 監視（2時間〜24時間）

- [ ] 5xx比率、タイムアウト、重大例外の監視。
- [ ] ログイン失敗率の急増がないことを確認。
- [ ] 決済関連イベント失敗（署名検証失敗/Price不一致）を監視。
- [ ] 問い合わせ窓口に重大障害報告がないか確認。

---

## 5. Go / No-Go 判定

### 5.1 Go 条件

- [ ] health/API/Frontend が安定稼働。
- [ ] 認証・ゲストチャット・FAQ・請求導線が動作。
- [ ] 重大ログ（クリティカル）が許容範囲内。

### 5.2 No-Go 条件

- [ ] DBマイグレーション失敗、または不整合発生。
- [ ] ログイン不能・API 5xx連続発生。
- [ ] Stripe連携の致命障害（課金状態不整合、Webhook連続失敗）。

No-Go 1件でも発生した場合は、即座にロールバック判断へ移る。

---

## 6. ロールバック手順（緊急時）

1. Ownerがロールバック実行を決裁。  
2. `main` を直前安定コミットへ戻し再デプロイ。  
3. 必要時は本番DBを事前バックアップ地点へ復元。  
4. サービス復旧確認（health、ログイン、主要画面）。  
5. 事故記録（発生時刻、原因、対応、再発防止）を文書化。

---

## 7. 実施記録テンプレート

- 実施日:
- 実施者:
- 確認者:
- 対象コミット:
- 開始時刻:
- 完了時刻:
- Go/No-Go判定:
- 判定理由:
- インシデント有無:
- 備考:

---

**Document Version**: 1.1  
**Last Updated**: 2026年3月19日  
**Status**: §1.4〜1.5（品質ゲート・Stripe本番前確認）、§3.4 拡充。実装計画 v1.2 と整合。
