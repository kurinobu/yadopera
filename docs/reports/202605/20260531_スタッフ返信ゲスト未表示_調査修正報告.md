# スタッフ返信がゲスト画面に届かない — 調査・修正報告

**日付**: 2026-05-31  
**ブランチ**: `feature/guest-staff-reply-polling`  
**バックアップ**: `backup/20260531-guest-staff-polling-pre` / `docs/evidence/backup/20260531_guest_staff_polling_pre/`

---

## 1. 事象

| 項目 | 内容 |
|------|------|
| session_id | `824b1a50-45bb-4a74-b992-a4b8e72c4b20` |
| 施設 | facility_id=2（yadopera-mainホテル） |
| 管理画面 | 14:22:35 staff 返信成功・表示あり |
| ゲスト画面 | staff 未表示、AI のみ |

---

## 2. 原因（確定）

| レイヤ | 結果 |
|--------|------|
| DB / API | ✅ `role=staff` 保存・`GET /chat/history?facility_id=2` で取得可 |
| ゲスト UI | ❌ **B-2 未実装** — `loadHistory` は onMounted・トークン統合時のみ。ポーリングなし |

---

## 3. 実装内容（Phase B-2 最小）

| ファイル | 変更 |
|----------|------|
| `frontend/src/views/guest/Chat.vue` | 30秒間隔ポーリング（最大10分）、手動「更新」、エスカレーション後に開始 |
| `frontend/src/composables/useChat.ts` | `loadHistory(..., { silent })` — ポーリング時にローディングバーを出さない |
| `frontend/src/components/guest/ChatMessage.vue` | `role=staff` のラベル・amber スタイル |
| `frontend/src/utils/staffReplyGuestCopy.ts` | 案内文言（ja/en） |

---

## 4. 検証手順

1. Docker でゲストチャット → スタッフに連絡（エスカレーション）
2. 別タブで管理画面から staff 返信
3. **ゲストタブをリロードせず** 30秒以内に staff 吹き出しが表示されること
4. 「更新」ボタンで即時反映されること

---

## 5. デプロイ

`develop` → staging 確認 → `main` → Dokploy 本番（Auto Deploy 復旧済み: `docs/evidence/20260531_dokploy_autodeploy_security_group_remediation.md`）
