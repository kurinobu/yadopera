# C-3 連絡先同意機能 ステージング実施記録

## 0. 対象Runbook

- `docs/operations/202604/20260423_C3_連絡先同意機能_ステージング検証手順.md`

## 1. 実施メタ情報

- 実施日時: 2026-04-23 18:33 JST
- 実施者: Codex（with user-provided staging credentials）
- 対象環境: staging
- 対象ブランチ / リビジョン:
- Backend フラグ値（`ENABLE_CONTACT_CAPTURE`）: OFF相当（`/chat/contact-consent` が 404）
- Frontend フラグ値（`VITE_ENABLE_CONTACT_CAPTURE`）: 未確認
- 実施ステータス: 実施中（API二次確認まで完了）
- 検証コマンド実行値:
  - `BASE_URL`: `https://yadopera-backend-staging.onrender.com`
  - `FACILITY_ID`: `393`
  - `SESSION_ID`: `25483491-47b2-4df4-9c18-6e86a89d7a4f`
  - `ADMIN_BEARER_TOKEN`（末尾4桁のみ）: `26Uc`

## 1.1 事前確認（ローカル）

- [x] Backend 対象テスト通過（B-1/C-3）
- [x] Frontend build 実行（既知PWAエラーを除く）
- [x] Manual.vue 変更を含む lint/type エラーなし
- メモ:
  - Backend: B-1/C-3 の対象5テストが `5 passed`。
  - Frontend: `npm run build` 実行。既知の `vite-plugin-pwa/workbox` service worker 生成エラーは継続（運用上の既知事象）。
  - Lint: 対象変更ファイルでエラーなし。

## 2. ケースA（フラグOFF）結果

### 2.1 実施内容

- [ ] ゲスト画面で質問送信
- [ ] スタッフ連絡（エスカレーション）実行
- [ ] 受付完了モーダル確認
- [ ] 管理画面未解決一覧確認
- [ ] 会話詳細で手動返信

### 2.2 判定

- ケースA判定: API観測はFail（B-1 API 404）
- 観測メモ:
  - 同意フォーム表示有無: 未確認（UI未実施）
  - `/api/v1/chat/contact-consent` 応答: `404 Not Found`（OFF相当）
  - 連絡可能状態: `POST /api/v1/chat/escalate` 実行後、`GET /api/v1/chat/history/{session_id}` は `200` かつ `unresolved_escalation_id: 96`
  - 手動返信API（B-1）: `POST /api/v1/admin/conversations/{session_id}/reply` は `404 Not Found`（未解決エスカレーション有でも404）

## 3. ケースB（フラグON）結果

### 3.1 実施内容

- [ ] ゲスト画面でスタッフ連絡実行
- [ ] 同意フォーム表示確認
- [ ] メール + 同意で送信
- [ ] 未解決一覧再読み込み
- [ ] 会話詳細で連絡可能状態確認
- [ ] 管理画面で手動返信

### 3.2 判定

- ケースB判定: Pass / Fail
- 観測メモ:
  - 同意なし送信制御:
  - `/api/v1/chat/contact-consent` 応答:
  - 一覧/詳細の状態一致:
  - B-1 返信導線との干渉有無:

## 4. No-Go 判定

- No-Go 判定: Yes（現時点）
- 理由: ケースB（フラグON）とUI確認が未実施、かつB-1 APIが未解決エスカレーション有の状態でも404
- 発生事象:
  - `POST /api/v1/chat/escalate` は成功（`escalation_id: 96`）。
  - `POST /api/v1/admin/conversations/{session_id}/reply` は `404`（ステージング未反映の疑い）。
  - `GET /openapi.json` の `paths` には `/api/v1/chat/escalate` のみ存在し、`/api/v1/chat/contact-consent` と `/api/v1/admin/conversations/{session_id}/reply` は未掲載。

## 5. ロールバック実施記録

- 実施有無: Yes / No
- 実施時刻:
- 手順:
  1. Frontend フラグ OFF
  2. Backend フラグ OFF
  3. ゲスト画面同意フォーム非表示確認
  4. B-1 手動返信導線確認
- 結果:

## 6. 最終判断

- 本番反映可否: No-Go（現時点）
- 条件付きGoの場合の条件:
  - ケースB（ON）で `/chat/contact-consent` の `200` を確認
  - 未解決エスカレーションを作成したセッションで `reply` が `201` になることを確認（現状未達）
  - `/admin/manual` 反映確認4項目をPassにすること
- 次アクション:
  - ケースA/BのUI実施と証跡取得
  - ステージングBackendのデプロイリビジョン確認（`/admin/conversations/{session_id}/reply` ルート有無）
  - 必要に応じて未解決エスカレーション作成用の検証セッションを再作成

## 7. マニュアル更新反映確認（必須）

- 対象画面: `/admin/manual`
- 対象ファイル: `frontend/src/views/admin/Manual.vue`
- 反映確認手順:
  1. フロント再ビルド/再起動
  2. ブラウザのサイトデータ削除
  3. Service Worker Unregister
  4. ハードリロード（`Cmd+Shift+R`）
- 確認項目:
  - [ ] 第9章 会話詳細で「スタッフ」メッセージ種別が説明されている
  - [ ] 第9.4 で手動返信が実装済みである説明に更新されている
  - [ ] 受付番号突合と連絡可能状態確認の運用が明記されている
  - [ ] 第12.2 日次運用にエスカレーション実務フローが反映されている
- 判定: Pass / Fail
- メモ:

## 8. 証跡

- スクリーンショット:
- APIレスポンスログ:
  - `POST /api/v1/chat/contact-consent` -> `404 Not Found`（2026-04-23 18:33 JST）
  - `POST /api/v1/chat/escalate` -> `200 OK`（`escalation_id: 96`）
  - `GET /api/v1/chat/history/{session_id}` -> `200 OK`（`facility_id: 393`, `unresolved_escalation_id: 96`）
  - `POST /api/v1/admin/conversations/{session_id}/reply` -> `404 Not Found`（未解決エスカレーション有でも再現）
  - `GET /openapi.json` -> `paths` に `contact-consent` / `admin/conversations` が存在しないことを確認
- 監視ログ参照先:
- 実行コマンドテンプレート使用有無: Yes / No
- コマンドテンプレート: `docs/operations/202604/20260423_C3_ステージング検証コマンドテンプレート.sh`

