# 2026-03-24 本番動作テスト実行記録（§6.0 再判定用）

## 1. 目的

- Stripe live 投入前提として、§6.0 の不足項目を本番環境で検証する。
- 「ステージング実施済み」ではなく「本番実測」の証跡を残す。

---

## 2. 前提

- 本番対象:
  - Frontend: `https://yadopera-frontend-production.onrender.com`
  - Backend: `https://yadopera-backend-production.onrender.com`
- Stripe:
  - Render 本番 Backend の `STRIPE_*` は未設定（Owner 指示どおり）。
  - Stripe live の Product / Price / Webhook は未作成。
- 実施日: 2026-03-24
- 実施者: AI Assistant（Owner 指示の範囲で実施）

---

## 3. 実施計画（今回の対象）

1. 本番疎通（health / 主要URL）
2. 本番管理者導線（ログイン、主要画面）
3. 本番管理者API導線（dashboard / faqs / plans / invoices / leads）
4. 本番ゲスト導線（chat / history）
5. 本番開発者導線（developer auth）
6. 本番FAQ CSV一括登録（方法D）
7. 本番FAQ CSV一括登録（管理者導線）

---

## 4. 実行結果

### 4.1 本番疎通（実行済み）

- `GET https://yadopera-backend-production.onrender.com/api/v1/health` -> `200`
- `GET https://yadopera-backend-production.onrender.com/` -> `200`
- `GET https://yadopera-frontend-production.onrender.com/` -> `200`
- `GET https://yadopera-frontend-production.onrender.com/admin/login` -> `200`
- `GET https://yadopera-frontend-production.onrender.com/f/384` -> `200`

判定: **OK**

### 4.2 本番管理者導線（実行済み）

- 本番管理者ログイン画面で `info@yadobito.com` にてログイン成功。
- 遷移先: `/admin/dashboard` を確認。
- サイドメニュー表示（FAQ管理 / プラン・請求 / CSV代行 等）を確認。

判定: **OK（ログイン〜ダッシュボード到達）**

### 4.3 本番管理者API導線（実行済み）

`POST /api/v1/auth/login` 後の Bearer トークンで下記を確認:

- `GET /api/v1/auth/me` -> `200`
- `GET /api/v1/admin/dashboard` -> `200`
- `GET /api/v1/admin/faqs` -> `200`
- `GET /api/v1/admin/plans` -> `200`
- `GET /api/v1/admin/invoices` -> `200`
- `GET /api/v1/admin/leads` -> `200`

判定: **OK**

### 4.4 本番ゲスト導線（実行済み）

- `POST /api/v1/chat` -> `200`
  - facility_id: `386`
  - session_id: `bac8e2bf-21ee-4efb-9c41-b2d1af2e4b00`
- `GET /api/v1/chat/history/{session_id}?facility_id=386` -> `200`
  - messages: `2`

判定: **OK**

### 4.5 本番開発者導線（部分実施）

- `POST /api/v1/developer/auth/login` を実施。
- 正しい `DEVELOPER_PASSWORD` により `200` を確認。
- 開発者APIを実測:
  - `GET /api/v1/developer/stats/overview` -> `200`
  - `GET /api/v1/developer/stats/facilities` -> `200`
  - `GET /api/v1/developer/health/system` -> `200`
  - `GET /api/v1/developer/errors/list?page=1&per_page=5` -> `200`
- 本番フロント (`/developer/login`) からログイン後、`/developer/dashboard` の表示を確認（統計カード・施設一覧・CSV導線表示）。

判定: **OK**

### 4.6 本番FAQ CSV一括登録（方法D）

- 対象CSV: `/Users/kurinobu/Downloads/FAQ_CSV_template_4lang_test_v3.csv`
- 実施:
  - 方法D (`/api/v1/developer/facilities/{facility_id}/faqs/bulk-upload`) を開発者JWTで実行。
  - 試行1: `facility_id=370 (Premium)` は `400`（同一FAQ重複エラー）。
  - 試行2: `facility_id=371 (Premium)` は `201` 成功。
  - 結果: `success_count=15`, `failure_count=0`, `total_count=15`, `processing_time_seconds=51.55`

判定: **OK（方法D 実行成功）**

### 4.7 本番FAQ CSV一括登録（管理者導線、参考実測）

- 実施:
  - `POST /api/v1/admin/faqs/bulk-upload`
  - CSV: `/Users/kurinobu/Downloads/FAQ_CSV_template_4lang_test_v3.csv`
  - mode: `add`
- 結果:
  - HTTP: `201`
  - `success_count=15`
  - `failure_count=0`
  - `total_count=15`
  - `processing_time_seconds=48.92`

判定: **OK（管理者導線のCSV一括登録は本番で動作）**

### 4.8 §1.5 着手時点の Render 本番 `STRIPE_*` 現況

- Render: `yadopera-backend-production` → Environment を確認。
- `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET` / `STRIPE_PUBLISHABLE_KEY` / `STRIPE_PRICE_ID_*` は **未設定（空）** を確認。
- 判定: **§1.5 の前提と整合**（live値の投入前）。

### 4.9 §1.5 Stripe live 反映（Owner 共同実施）

- Stripe ダッシュボード（live）で以下を実施:
  - Product/Price（`Mini` / `Small` / `Standard` / `Premium` / `従量課金`）の `price_...` を確認。
  - Webhook 送信先を新UIで作成。
  - Webhook URL: `https://yadopera-backend-production.onrender.com/api/v1/webhooks/stripe`
  - リッスン対象: `customer.subscription.created` / `customer.subscription.updated` / `customer.subscription.deleted` / `invoice.paid` / `invoice.payment_failed`
- Render 本番 `yadopera-backend-production` Environment:
  - `STRIPE_PUBLISHABLE_KEY` / `STRIPE_SECRET_KEY` / `STRIPE_WEBHOOK_SECRET`
  - `STRIPE_PRICE_ID_MINI` / `STRIPE_PRICE_ID_SMALL` / `STRIPE_PRICE_ID_STANDARD` / `STRIPE_PRICE_ID_PREMIUM` / `STRIPE_PRICE_ID_USAGE_PER_QUESTION`
  - 上記を live 値へ更新し保存（値そのものは記録しない）。
- 保存後、Render で自動デプロイ完了を確認。
- 反映後の疎通確認（2026-03-24）:
  - `GET https://yadopera-backend-production.onrender.com/api/v1/health` -> `200`
  - `GET https://yadopera-backend-production.onrender.com/` -> `200`
  - `GET https://yadopera-backend-production.onrender.com/api/v1/webhooks/stripe` -> `405`（想定どおり）
  - `POST https://yadopera-backend-production.onrender.com/api/v1/webhooks/stripe`（署名なし）-> `400`（署名検証が有効）

判定: **OK（liveキー反映後の基本疎通は正常。Webhook 受信口は署名必須で稼働）**

### 4.10 §3.4 無課金テスト（Owner 方針: 実課金テスト回避）

- 背景:
  - Premium 等の live 実決済を複数回行うと高額課金となるため、**Owner 決定で実課金テストは実施しない**。
- 実施（無課金で実行可能な範囲）:
  - `GET https://yadopera-backend-production.onrender.com/api/v1/health` -> `200`
  - `GET https://yadopera-frontend-production.onrender.com/admin/login` -> `200`
  - `GET https://yadopera-backend-production.onrender.com/api/v1/webhooks/stripe` -> `405`（エンドポイント到達 + GET 非許可）
  - `POST https://yadopera-backend-production.onrender.com/api/v1/webhooks/stripe`（署名なし）-> `400`（署名検証が有効）
- 判定:
  - **Webhook受信口・署名検証・本番疎通は無課金で確認済み。**
  - **実課金を伴う E2E（新規契約/変更/解約/請求確定）は実施対象外（Owner 決定）。**

### 4.11 Stripe 審査ブロッカーの認識合わせ（Owner 相談）

- 観測:
  - Stripe ダッシュボードの `アカウントのステータス` で「追加情報を提供してください」「支払いを一時停止」と表示。
  - メール通知でも最新Webサイト提出依頼（特定取引法に基づく表記ページに関する案内）を確認。
- 分析:
  - 実課金 E2E の前に、審査未完了起因の失敗ノイズを排除する必要がある。
  - 現行 `landing/index.html` は法務ページリンクがある一方で、CTA が `https://yadopera-frontend-staging.onrender.com/admin/register` を指しており、審査提示用としては運用意図が不明瞭になりうる。
- 方針（採用）:
  - `main` / GitHub Pages で LP + legal 一式を審査提示しつつ、アプリ導線は一時停止（審査解除まで）。
  - Stripe 審査解除後に §3.4 の最終判定へ進む。
- 2026-03-24 進捗:
  - `yadopera.com` および legal URL を Stripe に提出。
  - Stripe ダッシュボードのステータスが **審査中** に遷移したことを確認。

---

## 5. 残存課題（Stripe live 前の必須）

1. Stripe 審査ブロッカー解消（**提出完了・審査中**。解除確認待ち）
2. 審査提示用 Web（`main` の `landing/`）を整備し、アプリ導線一時停止状態で公開
3. Stripe Webhook 配信ログでエラー多発がないことの確認（監視継続）
4. 必要なら監査ログ（`developer_faq_bulk_upload`）の最終確認（本番DB参照）
5. Owner 最終 Go/No-Go 判定

※ 4.6/4.7 で投入したテストFAQおよびPoC由来データは、Owner 指示により本番DBで全削除を実施済み。  
※ 実施内容: `public` スキーマの `alembic_version` を除く全テーブルを `TRUNCATE ... RESTART IDENTITY CASCADE`。  
※ 実測: `users / facilities / messages / conversations / faqs / faq_translations / admin_activity_logs / guest_leads / error_logs` など主要テーブルは `count(*)=0`、全25テーブル合計 `total_rows_remaining=0`。

---

## 6. Owner 手動で実施が必要な項目

- 実課金を伴う Stripe live 実体テストは実施しない（Owner 決定）
- 最終 Go/No-Go 判定
- 必要時のみ: 本番DBで `admin_activity_logs` の `action_type='developer_faq_bulk_upload'` を確認

---

## 7. 判定

- §6.0: **完了（Owner Go）**
- Stripe live 投入: **実施済み（Render 本番 `STRIPE_*` live反映完了）**
- 次フェーズ: **Stripe 審査ブロッカー解消（Web提示整備）を先行し、その後 §3.4 最終判定**

---

## 8. 重大インシデント追記（2026-03-24）

- 事象: ステージング開発者ページの施設一覧が 0 件になる障害が発生。
- 原因: `TRUNCATE ... RESTART IDENTITY CASCADE` が Railway 接続先（`tramway.proxy.rlwy.net:50673/railway`）に対して実行され、ステージング運用データが全削除された。
- 復旧:
  - 退避: `yadopera_staging_before_restore_20260324_1835.dump`
  - 復元: `yadopera_prod_20260323.dump` を `pg_restore --clean --if-exists` で復元
  - 復旧確認: `users=27`, `facilities=28`, `faqs=629`, `conversations=1154`, `messages=2490`
- 詳細報告:
  - `docs/20260324_ステージングDB全消失インシデント_原因調査報告.md`

