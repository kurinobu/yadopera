# スマートフォン実機で画面が真っ白になる問題 - 調査依頼・修正方法依頼書

**作成日時**: 2025年12月17日 18時10分48秒  
**問題の重大度**: 🔴 **非常に高い（クリティカル）**  
**対象**: ChatGPT / Cursor AI  
**目的**: 問題の原因を調査分析し、修正方法を提案する

---

## 1. プロジェクト概要

### 1.1 アプリケーションの概要

**プロジェクト名**: やどぺら（Yadopera）  
**種類**: 小規模宿泊施設向けAI多言語自動案内システム  
**アーキテクチャ**: RAG (Retrieval-Augmented Generation)

### 1.2 技術スタック

**フロントエンド**:
- **フレームワーク**: Vue.js 3 (Composition API)
- **言語**: TypeScript
- **状態管理**: Pinia
- **ビルドツール**: Vite 5.0.12
- **ルーティング**: Vue Router 4
- **UIフレームワーク**: Tailwind CSS
- **PWA**: vite-plugin-pwa 0.19.8
- **HTTPクライアント**: Axios 1.6.8

**バックエンド**:
- **フレームワーク**: FastAPI (Python)
- **データベース**: PostgreSQL (pgvector extension)
- **キャッシュ**: Redis
- **認証**: JWT

**デプロイ環境**:
- **ステージング環境**: Render.com
  - フロントエンド: Render.com Static Site
  - バックエンド: Render.com Web Service
- **ローカル開発環境**: Docker / docker-compose

### 1.3 主要な機能

- ゲスト向けチャットインターフェース（多言語対応）
- 管理画面（FAQ管理、ダッシュボード、設定など）
- PWA（Progressive Web App）対応
- セッション管理（4桁英数字トークン）
- ゲストフィードバック（👍👎）
- FAQ自動学習機能

---

## 2. 問題の詳細

### 2.1 問題の概要

**問題**: スマートフォン実機とタブレットでアクセスすると、画面が真っ白になる

**重大度**: 🔴 **非常に高い（クリティカル）**  
**影響範囲**: ゲスト向けインターフェース全体  
**発生環境**: ステージング環境（Render.com）のみ

### 2.2 発生環境と現象

#### 2.2.1 発生環境

**テスト環境**:
- **ステージングURL**: `https://yadopera-frontend-staging.onrender.com/f/test-facility?location=entrance`
- **テストデバイス**:
  - スマートフォン実機（1台）
  - タブレット（iPad、GoogleChromeブラウザ）（1台）

**発生しない環境**:
- ローカルDocker環境（`http://localhost:5173`）では正常に動作
- デスクトップブラウザ（Chrome、Firefox、Safari）では正常に動作

#### 2.2.2 発生する現象

1. **画面が真っ白になる**
   - スマートフォン実機でアクセスすると、画面が完全に真っ白
   - タブレット（iPad、GoogleChromeブラウザ）でも同様の現象

2. **「test-facility.txt」ダウンロードポップアップ**（タブレットのみ）
   - タブレット（iPad、GoogleChromeブラウザ）でアクセスすると、「test-facility.txt」ダウンロードというポップアップが表示される
   - ダウンロードしたファイルは真っ白で何も表示されない

3. **サーバーログでは正常**
   - バックエンドのサーバーログでは、すべてのAPIリクエストが200 OKで返されている
   - エラーレスポンスは発生していない

#### 2.2.3 サーバーログの詳細

**正常に動作しているAPI**:
- `/api/v1/auth/me`: 200 OK（複数回）
- `/api/v1/facility/test-facility`: 200 OK（複数回）
- `/api/v1/session/generate`: 200 OK
- `/api/v1/chat/history`: 200 OK
- `/api/v1/chat`: 200 OK

**重要な観察**:
- バックエンドは正常に動作している
- APIリクエストはすべて成功している
- フロントエンドのレンダリングが失敗している

---

## 3. 実施した調査分析と判明事項

### 3.1 初期調査分析

#### 3.1.1 考えられた原因

1. **localStorageアクセスエラー**（可能性: 高い）
   - スマートフォンでlocalStorageが利用できない場合（プライベートモードなど）
   - エラーハンドリングがないと、アプリケーションが起動しない可能性

2. **window.matchMedia互換性問題**（可能性: 中）
   - 古いスマートフォンブラウザで`window.matchMedia`がサポートされていない可能性

3. **Vueアプリケーションのマウント失敗**（可能性: 高い）
   - `app.mount('#app')`が実行されていない、または失敗している可能性

4. **Content-Typeヘッダーの問題**（可能性: 高い）
   - サーバーがHTMLファイルを`text/plain`として配信している可能性
   - 「test-facility.txt」ダウンロードポップアップが発生

### 3.2 実施した修正案と結果

#### 修正案1: theme.tsのlocalStorageアクセスエラーハンドリング追加 ✅ 実施済み

**対象ファイル**: `frontend/src/stores/theme.ts`

**修正内容**:
- `initTheme()`関数に`try-catch`を追加
- `updateTheme()`関数に`try-catch`を追加
- `window.matchMedia`の互換性チェックを追加

**結果**: ❌ **問題が解決していない**

#### 修正案2: auth.tsのlocalStorageアクセスエラーハンドリング追加 ✅ 実施済み

**対象ファイル**: `frontend/src/stores/auth.ts`

**修正内容**:
- `setToken()`関数に`try-catch`を追加
- `initAuth()`関数に`try-catch`を追加

**結果**: ❌ **問題が解決していない**

#### 修正案3: main.tsの初期化処理エラーハンドリング追加 ✅ 実施済み

**対象ファイル**: `frontend/src/main.ts`

**修正内容**:
- `themeStore.initTheme()`を`try-catch`で囲む

**結果**: ❌ **問題が解決していない**

#### 修正案5: PWAInstallPrompt.vueのlocalStorageアクセスエラーハンドリング追加 ✅ 実施済み

**対象ファイル**: `frontend/src/components/common/PWAInstallPrompt.vue`

**修正内容**:
- スクリプトトップレベルでの`localStorage.getItem`を`try-catch`で囲む
- `dismiss()`関数内の`localStorage.setItem`を`try-catch`で囲む

**結果**: ❌ **問題が解決していない**

#### 追加修正案: usePWA.tsのwindow.matchMedia互換性チェック追加 ✅ 実施済み

**対象ファイル**: `frontend/src/composables/usePWA.ts`

**修正内容**:
- `checkIfInstalled()`関数に`try-catch`と`window.matchMedia`の存在チェックを追加

**結果**: ❌ **問題が解決していない**

#### 修正案7・8: main.tsの初期化処理を改善（タイムアウト設定） ✅ 実施済み

**対象ファイル**: `frontend/src/main.ts`

**修正内容**:
- すべての初期化処理を非同期関数`initializeApp()`にまとめる
- タイムアウト（2秒）を設定し、初期化が完了しない場合でも強制的に`app.mount('#app')`を実行

**結果**: ❌ **問題が解決していない**

#### 修正案9: _headersファイルを作成し、Content-Typeヘッダーを設定 ✅ 実施済み

**対象ファイル**: `frontend/public/_headers`

**修正内容**:
- HTMLファイルのContent-Typeを`text/html; charset=utf-8`に設定
- SPAのフォールバックルート（`/*`）にもContent-Typeを設定
- 静的リソース（JavaScript、CSS、画像など）のContent-Typeも設定

**結果**: ❌ **問題が解決していない**

#### 修正案10: vite.config.tsの設定を確認 ✅ 実施済み

**対象ファイル**: `frontend/vite.config.ts`

**修正内容**:
- 設定を確認（問題なし、修正不要）

**結果**: ❌ **問題が解決していない**

#### 修正案11: Render.comダッシュボードでの手動設定 ✅ 実施済み

**対象**: Render.comダッシュボードの「Headers」タブ

**修正内容**:
- Render.comダッシュボードで静的サイトの「Headers」タブを開く
- Path: `/*`、Header Name: `Content-Type`、Header Value: `text/html; charset=utf-8`を手動設定

**結果**: ❌ **問題が解決していない**

#### 修正案12: デスクトップブラウザでのエラー確認 ✅ 実施済み

**対象**: デスクトップブラウザの開発者ツール

**修正内容**:
- デスクトップブラウザで同じURLにアクセス
- 開発者ツールでエラーを確認

**結果**: ❌ **問題が解決していない**（詳細なエラー情報は未取得）

#### 修正案13: ビルド後のファイルの確認 ✅ 実施済み

**対象**: ビルド後の`dist`ディレクトリ

**修正内容**:
- ローカル環境で`npm run build`を実行
- `dist`ディレクトリの内容を確認

**結果**: ✅ **ビルドプロセスとファイル生成に問題なし**
- `dist/index.html`が正しく生成されている
- `dist/_headers`が正しくコピーされている
- `dist/_redirects`が正しくコピーされている
- すべての静的リソースが正しく生成されている

### 3.3 判明事項のまとめ

**判明したこと**:
1. ✅ バックエンドは正常に動作している（APIリクエストはすべて成功）
2. ✅ ビルドプロセスとファイル生成に問題はない
3. ✅ ローカルDocker環境では正常に動作する
4. ✅ デスクトップブラウザでは正常に動作する
5. ❌ スマートフォン実機とタブレットでは画面が真っ白になる
6. ❌ 実施したすべての修正案で問題が解決していない

**未判明のこと**:
1. ❓ スマートフォンでJavaScriptの実行エラーが発生しているか
2. ❓ Vueアプリケーションのマウントが失敗しているか
3. ❓ 静的リソースの読み込みに失敗しているか
4. ❓ Render.comでのデプロイプロセスに問題があるか
5. ❓ `_headers`ファイルがRender.comで正しく認識されているか

---

## 4. コードベースの重要な情報

### 4.1 主要なファイル構成

**フロントエンドの主要ファイル**:
```
frontend/
├── src/
│   ├── main.ts                    # アプリケーションのエントリーポイント
│   ├── App.vue                    # ルートコンポーネント
│   ├── router/
│   │   └── index.ts               # Vue Router設定
│   ├── stores/
│   │   ├── auth.ts                # 認証状態管理（localStorage使用）
│   │   ├── theme.ts               # テーマ状態管理（localStorage使用）
│   │   └── chat.ts                # チャット状態管理
│   ├── composables/
│   │   ├── usePWA.ts             # PWA機能（window.matchMedia使用）
│   │   └── useSession.ts          # セッション管理
│   └── views/
│       └── guest/
│           └── Chat.vue           # ゲスト向けチャット画面
├── public/
│   ├── _headers                   # HTTPヘッダー設定（新規作成）
│   └── _redirects                 # SPAルーティング設定
├── index.html                     # HTMLエントリーポイント
└── vite.config.ts                 # Vite設定
```

### 4.2 main.tsの現在の実装

```typescript
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import router from './router'
import App from './App.vue'
import './style.css'
import { useThemeStore } from './stores/theme'
import { useAuthStore } from './stores/auth'

const app = createApp(App)
const pinia = createPinia()

app.use(pinia)
app.use(router)

// 初期化処理（すべて非同期で実行）
async function initializeApp() {
  try {
    const themeStore = useThemeStore()
    themeStore.initTheme()
  } catch (error) {
    console.error('Failed to initialize theme:', error)
    // エラーが発生してもアプリは起動する
  }

  try {
    const authStore = useAuthStore()
    await authStore.initAuth()
  } catch (error) {
    console.error('Failed to initialize auth:', error)
    // エラーが発生してもアプリは起動する
  }

  // すべての初期化処理が完了したら、アプリをマウント
  app.mount('#app')
}

// タイムアウトを設定し、一定時間経過後は強制的にアプリをマウント
const mountTimeout = setTimeout(() => {
  console.warn('App initialization timeout, mounting app anyway')
  app.mount('#app')
}, 2000) // 2秒後にタイムアウト

initializeApp()
  .then(() => {
    clearTimeout(mountTimeout)
  })
  .catch((error) => {
    clearTimeout(mountTimeout)
    console.error('Failed to initialize app:', error)
    // エラーが発生してもアプリは起動する
    app.mount('#app')
  })
```

### 4.3 index.htmlの内容

```html
<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="小規模宿泊施設向けAI多言語自動案内システム" />
    <title>やどぺら</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

### 4.4 _headersファイルの内容

```
# HTTP Headers for Render.com Static Site
# This file configures Content-Type headers to ensure HTML files are served correctly

# HTML files
/*.html
  Content-Type: text/html; charset=utf-8

# All routes (SPA fallback)
/*
  Content-Type: text/html; charset=utf-8

# JavaScript files
/*.js
  Content-Type: application/javascript; charset=utf-8

# CSS files
/*.css
  Content-Type: text/css; charset=utf-8

# JSON files
/*.json
  Content-Type: application/json; charset=utf-8

# SVG files
/*.svg
  Content-Type: image/svg+xml

# PNG files
/*.png
  Content-Type: image/png

# ICO files
/*.ico
  Content-Type: image/x-icon
```

### 4.5 _redirectsファイルの内容

```
/*    /index.html   200
```

### 4.6 vite.config.tsの設定

```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { VitePWA } from 'vite-plugin-pwa'
import { fileURLToPath, URL } from 'node:url'

export default defineConfig({
  base: '/',
  plugins: [
    vue(),
    VitePWA({
      registerType: 'autoUpdate',
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg}'],
        runtimeCaching: [
          {
            urlPattern: /\/api\/v1\/admin\/.*$/,
            handler: 'NetworkOnly',
            method: 'GET'
          }
        ]
      },
      manifest: {
        name: 'やどぺら',
        short_name: 'やどぺら',
        description: '小規模宿泊施設向けAI多言語自動案内システム',
        theme_color: '#ffffff',
        icons: [
          {
            src: 'pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          }
        ]
      }
    })
  ],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url))
    }
  },
  server: {
    host: '0.0.0.0',
    port: 5173,
    watch: {
      usePolling: true
    }
  }
})
```

---

## 5. デプロイ環境の詳細

### 5.1 Render.com Static Site設定

**設定項目**:
- **Name**: `yadopera-frontend-staging`
- **Branch**: `develop`
- **Root Directory**: `frontend`
- **Build Command**: `npm run build`
- **Publish Directory**: `dist`

**環境変数**:
- `VITE_API_BASE_URL=https://yadopera-backend-staging.onrender.com`
- `VITE_ENVIRONMENT=staging`

### 5.2 ビルド結果

**ビルドログ**:
- TypeScriptコンパイル: 成功
- Viteビルド: 成功（1.13秒）
- 生成されたファイル数: 37エントリ（371.61 KiB）
- PWAファイル生成: 成功

**生成された主要ファイル**:
- `dist/index.html` - 正しく生成されている
- `dist/_headers` - 正しくコピーされている
- `dist/_redirects` - 正しくコピーされている
- `dist/assets/index-Dt3rQ5Yr.js` - メインJavaScriptファイル（164.74 kB）
- `dist/assets/index-BWPcFWvR.css` - メインCSSファイル（34.38 kB）

---

## 6. その他の留意点

### 6.1 大原則（開発方針）

このプロジェクトでは以下の大原則に従って開発を行っています：

1. **根本解決 > 暫定解決**: 根本的な解決を優先
2. **シンプル構造 > 複雑構造**: シンプルな構造を優先
3. **統一・同一化 > 特殊独自**: 統一された実装を優先
4. **具体的 > 一般**: 具体的な実装を優先
5. **拙速 < 安全確実**: 安全確実な実装を優先
6. **Docker環境必須**: すべての修正・テストはDocker環境で実施

### 6.2 制約事項

1. **スマートフォンでは開発者ツールが使用できない**
   - コンソールエラーを直接確認できない
   - ネットワークタブで静的リソースの読み込み状況を確認できない

2. **Render.comの静的サイト設定**
   - `_headers`ファイルがサポートされているか不明
   - ダッシュボードでの手動設定も試したが、問題が解決していない

3. **デスクトップブラウザでは正常に動作する**
   - 問題はスマートフォン固有の可能性が高い
   - モバイルブラウザの制約や互換性の問題の可能性

### 6.3 追加で確認すべき点

1. **JavaScriptの実行エラー**
   - スマートフォンでJavaScriptファイルの実行に失敗している可能性
   - コンソールエラーが発生している可能性

2. **Vueアプリケーションのマウント失敗**
   - `app.mount('#app')`が実行されていない、または失敗している可能性
   - `#app`要素が存在しない可能性

3. **静的リソースの読み込み失敗**
   - JavaScriptファイルやCSSファイルの読み込みに失敗している可能性
   - パスが正しく設定されていない可能性

4. **Render.comでのデプロイプロセス**
   - `_headers`ファイルが正しくアップロードされているか
   - デプロイ時にファイルが除外されていないか

5. **モバイルブラウザの制約**
   - iOS SafariのプライベートモードでのlocalStorage制限
   - モバイルブラウザでのJavaScript実行制限

---

## 7. 調査依頼事項

### 7.1 調査してほしいこと

1. **問題の根本原因の特定**
   - なぜスマートフォン実機とタブレットで画面が真っ白になるのか
   - デスクトップブラウザでは正常に動作するのに、なぜモバイルデバイスでは動作しないのか

2. **JavaScriptの実行状況**
   - スマートフォンでJavaScriptファイルが正しく読み込まれているか
   - JavaScriptの実行エラーが発生しているか
   - Vueアプリケーションのマウントが成功しているか

3. **静的リソースの読み込み状況**
   - JavaScriptファイルやCSSファイルが正しく読み込まれているか
   - ネットワークエラーが発生していないか

4. **Render.comでのデプロイプロセス**
   - `_headers`ファイルが正しく認識されているか
   - Content-Typeヘッダーが正しく設定されているか

5. **モバイルブラウザ固有の問題**
   - iOS SafariやAndroid Chromeでの制約や互換性の問題
   - モバイルブラウザでのJavaScript実行制限

### 7.2 修正方法の提案

1. **根本的な解決方法**
   - 問題の根本原因を解決する方法
   - 大原則に準拠した修正方法

2. **暫定的な解決方法**（根本解決が困難な場合）
   - 問題を回避する方法
   - 代替手段の提案

3. **検証方法**
   - 修正後の検証方法
   - テスト手順の提案

---

## 8. 参考資料

### 8.1 関連文書

- `docs/Phase1_Phase2_引き継ぎ書_20251214.md` - プロジェクトの引き継ぎ書
- `docs/Summary/yadopera-v03-summary.md` - プロジェクトの概要
- `docs/Phase1_Phase2_スマートフォン真っ白画面問題_完全調査分析_大原則準拠修正案_20251217.md` - 初期調査分析
- `docs/Phase1_Phase2_スマートフォン真っ白画面問題_修正後テスト結果_原因調査分析_20251217.md` - 修正案2実施後テスト結果
- `docs/Phase1_Phase2_スマートフォン真っ白画面問題_修正案2実施後テスト結果_原因再調査分析_20251217.md` - 修正案2実施後テスト結果
- `docs/Phase1_Phase2_スマートフォン真っ白画面問題_修正案7・8実施後テスト結果_原因再調査分析_20251217.md` - 修正案7・8実施後テスト結果
- `docs/Phase1_Phase2_スマートフォン真っ白画面問題_修正案9・10実施後テスト結果_原因再調査分析_20251217.md` - 修正案9・10実施後テスト結果
- `docs/Phase1_Phase2_スマートフォン真っ白画面問題_修正案13実施結果_20251217.md` - 修正案13実施結果

### 8.2 リポジトリ情報

- **GitHubリポジトリ**: `https://github.com/kurinobu/yadopera`
- **ブランチ**: `develop`
- **ステージング環境URL**: `https://yadopera-frontend-staging.onrender.com`

---

## 9. 連絡先・追加情報の提供

### 9.1 追加情報の提供

調査に必要な追加情報があれば、以下の方法で提供可能です：

1. **コードベースの詳細**
   - 特定のファイルの完全な内容
   - 関連する設定ファイルの内容

2. **ログ情報**
   - サーバーログの詳細
   - ビルドログの詳細

3. **環境情報**
   - Render.comの設定詳細
   - デプロイログの詳細

### 9.2 質問・確認事項

調査中に質問や確認事項があれば、遠慮なくお知らせください。

---

**作成日時**: 2025年12月17日 18時10分48秒  
**最終更新日時**: 2025年12月17日 18時10分48秒  
**状態**: 📋 **調査依頼書作成完了**

**重要**: この問題はクリティカルな問題であり、ゲスト向けインターフェース全体に影響を与えています。迅速な調査と解決方法の提案をお願いします。

