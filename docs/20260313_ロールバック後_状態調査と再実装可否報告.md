# ロールバック後 状態調査と再実装可否報告

**作成日**: 2026年3月13日  
**目的**: (1) 実装計画書の削除有無、(2) プラン超過時の挙動の完了状態、(3) ロールバック位置の妥当性、(4) CSV 関連3タスクの「最初から」実装開始可否を調査し報告する。

---

## 1. 「# CSV一括登録 マニュアル記載・有料オプション運用開始・フロート申し込み機能 実装計画」は削除したか

**回答: はい。作業ツリーからは削除しています。**

| 場所 | 有無 |
|------|------|
| **作業ツリー** `docs/` | **存在しない**（ロールバック時に `docs/CSV一括登録_マニュアル・有料オプション・フロート申し込み_実装計画.md` を削除済み） |
| **バックアップ** | **存在する** … `backups/20260313_before_rollback/docs/CSV一括登録_マニュアル・有料オプション・フロート申し込み_実装計画.md` |

再実装時に §4.1・§4.2・§4.3・料金表・フロー・§9 修正案などを参照するには、上記バックアップから `docs/` へ復元するか、バックアップを参照しながら進める必要があります。

---

## 2. 「プラン超過時の挙動」は全て完了した状態か

**回答: はい。全て完了した状態です。**

### 2.1 計画書の記載

- **ファイル**: `docs/プラン超過時の挙動_管理者選択制_実装計画.md`
- **Document Version**: 1.7  
- **Status**: 「Step 0〜Step 6 は 2026-03-13 に完了。Step 7（ドキュメント・マニュアル）は 2026-03-13 に完了。以上で本実装計画は**完了**。」

### 2.2 コード・DB・テストの存在

| 項目 | 状態 |
|------|------|
| マイグレーション | `022_add_overage_behavior_to_facilities.py` あり。`facilities.overage_behavior` 追加。 |
| Facility モデル | `overage_behavior` カラムあり（`backend/app/models/facility.py`）。 |
| ChatService | `_should_use_faq_only_path`・`_build_faq_only_response` で超過時 FAQ 限定パス・メーター報告なしを実装。 |
| DashboardService | `get_monthly_usage` で `overage_behavior` に応じ `status` を `faq_only` / `overage` に設定。 |
| 施設 API | 取得・更新で `overage_behavior` を返す／受け取る（billing / facility）。 |
| PlanBilling.vue | 「プラン超過時の挙動」の選択・保存 UI あり。 |
| 自動テスト | `test_chat_service.py`（faq_only/continue_billing）、`test_dashboard_service_billing_period.py`（status）で Step 6 検証。 |
| ドキュメント | 要約定義書・総括（`docs/20260307_プロジェクト現況と今後の計画_総括.md`）に「プラン超過時の挙動（管理者選択制）実装・デプロイ・ステージング動作確認完了」と記載。 |

ロールバックは「追跡済みファイルを fdd69d3 に復元」であり、fdd69d3 がプラン超過時実装完了コミットのため、**プラン超過時の挙動はロールバック後もそのまま完了状態**です。

---

## 3. 期待する位置にロールバックできているか

**回答: はい。期待する位置にロールバックできています。**

### 3.1 期待していた状態（報告書より）

- **ロールバック先**: コミット `fdd69d3`（feat: プラン超過時の挙動（管理者選択制）実装・ダッシュボード文言修正・ヘルプFAQ追加）
- **作業ツリー**: fdd69d3 の内容と一致し、CSV 一括登録・代行申し込み（Step 0〜3）に関する**変更・追加は一切ない**状態

### 3.2 現状の確認結果

| 確認項目 | 結果 |
|----------|------|
| HEAD | `fdd69d3` のまま |
| 追跡済みファイルの変更 | **なし**（`git status` に ` M` なし） |
| CsvBulkRequest.vue | **存在しない**（削除済み） |
| bulk_upload_faq_csv_for_facility.py | **存在しない**（削除済み） |
| backend/scripts/README.md | **存在しない**（削除済み） |
| test/ ディレクトリ | **存在しない**（削除済み） |
| docs/ 内の CSV 実装計画書 | **存在しない**（削除済み） |
| 20260313_* 実行計画・ロールバック報告・再発防止策 | 作業ツリーには**存在しない**（バックアップにあり） |
| CSV 代行用ルート・サイドバー・HelpModal リンク | **なし**（fdd69d3 の状態に復元済み） |

未追跡のまま残っているのは `backups/` 配下およびその他の未追跡ドキュメントのみで、**CSV Step 0〜3 に紐づくコード・設定・ドキュメントは作業ツリーからは除去済み**です。したがって、**期待する位置にロールバックできている**と判断できます。

---

## 4. この状態で「最初から」以下を実装開始できるか

対象タスク:

| タスク | 優先度 | 状態 | 内容 |
|--------|--------|------|------|
| **CSV一括登録 マニュアル記載** | 中 | 一部完了 | 使い方マニュアル詳細化、テンプレート配布 |
| **CSV一括登録 有料オプション運用開始** | 中 | 計画済み | 開発者代行サービス。手順・スクリプト済み。料金表は §4.1 参照 |
| **CSV一括登録 有料オプション フロート申し込み機能** | 中 | 未着手 | 有料代行の申し込みを画面上で受け付けるフロート（申し込みUI）の実装 |

**回答: 実装開始は可能ですが、事前に「実装計画書」を参照可能な状態にしておく必要があります。**

### 4.1 前提（事前にやること）

- **実装計画書の参照**:  
  `docs/CSV一括登録_マニュアル・有料オプション・フロート申し込み_実装計画.md` は現在作業ツリーにないため、次のいずれかが必要です。  
  - **推奨**: バックアップから `docs/` へ復元する。  
    - 元ファイル: `backups/20260313_before_rollback/docs/CSV一括登録_マニュアル・有料オプション・フロート申し込み_実装計画.md`  
  - 復元しない場合は、上記バックアップを参照しながら §4.1・§4.2・§4.3・§9 に沿って作業する。

- **フロート申し込みの再実装方針**:  
  Formspree は使わず、**バックエンド API + Brevo** で申し込みを受け付ける方針で「最初から」実装する。  
  - 実行計画・§9 修正案・原因と再発防止策は次のバックアップにある。  
    - `backups/20260313_before_rollback/docs/20260313_CSV一括登録_代行申し込み_FormspreeからAPI_Brevoへの修正_実行計画.md`  
    - 実装計画書の §9（同バックアップ内の実装計画書）

### 4.2 タスク別の実装開始可否

| タスク | 実装開始可否 | 補足 |
|--------|--------------|------|
| **マニュアル記載** | **可能** | Manual.vue の 4.8 は現状「対象プラン・画面の開き方・テンプレートリンク」等の基本記載のみ。実装計画書 §4.1 に従い、詳細化・エラー時・有料代行案内を「最初から」追記できる。テンプレート配布は既存の `/faq-csv-template/FAQ_CSV_template_4lang.csv` を継続利用可能。 |
| **有料オプション運用開始** | **可能** | 手順・料金表は実装計画書（バックアップ）の §4.1・§4.2 に記載。スクリプト `bulk_upload_faq_csv_for_facility.py` と運用開始チェックリストは `backups/20260313_before_rollback/backend/scripts/` および同 `docs/CSV_bulk_registration_function_implementation_plan/` にあり、復元するか計画に沿って再作成すれば運用開始の実装が可能。 |
| **フロート申し込み機能** | **可能** | 申し込み UI・ルート・リンクはロールバックで削除済み。実装計画書 §4.3 およびバックアップの「API+Brevo 実行計画」に従い、**バックエンド API（multipart 受付・Brevo 送信）→ フロント（申し込みページ・導線）** を最初から実装すればよい。ファイル添付は API で受け、Brevo で送る方針で再発防止策と整合する。 |

### 4.3 結論

- **実装計画書をバックアップから `docs/` に復元したうえで**、上記3タスクはいずれも「最初から」実装開始可能です。
- 復元するファイルの目安:
  - `CSV一括登録_マニュアル・有料オプション・フロート申し込み_実装計画.md`（必須）
  - 必要に応じて `20260313_CSV一括登録_代行申し込み_FormspreeからAPI_Brevoへの修正_実行計画.md`（フロート申し込みを API+Brevo でやるため）
- 有料オプション運用開始でスクリプト・チェックリストをそのまま使う場合は、`backups/20260313_before_rollback/` の該当ファイルをそれぞれ配置先に復元してから進めると効率的です。

以上。
