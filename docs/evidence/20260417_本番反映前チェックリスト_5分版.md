# 本番反映前チェックリスト（5分版）

**作成日**: 2026-04-17  
**目的**: ステージングで解消した「プラン・請求」障害の修正を、本番へ安全に反映する直前確認。

---

## 1. 対象コミット固定（30秒）

- [ ] 本番反映対象 SHA を固定した（推奨: `4628b4f` を含む先端）
- [ ] docs 変更（`d3fcc75`）を本番反映対象に含める/含めないを明示した

## 2. ステージング最終確認（90秒）

- [ ] `admin/billing` で `plans` が 200
- [ ] `admin/billing` で `invoices` が 200
- [ ] 画面表示（現在プラン、プラン一覧、請求履歴）が崩れていない

## 3. 起動健全性（60秒）

- [ ] backend `healthCheckPath` が 200（`/api/v1/health`）
- [ ] Render Logs に起動時 import エラーなし（`ModuleNotFoundError` なし）

## 4. 障害再発シグナル確認（60秒）

- [ ] backend logs で `Stripe Invoice list failed` が直近で増えていない
- [ ] backend logs で `/admin/invoices` の 500 が直近で出ていない

## 5. ロールバック即応性（60秒）

- [ ] 直前安定コミット SHA をメモした
- [ ] Render で Manual Deploy で戻す手順を再確認した
- [ ] 判定責任者（Owner）を明確化した

---

## 判定

- Go / No-Go:
- 判定時刻:
- 判定者:
- メモ:
