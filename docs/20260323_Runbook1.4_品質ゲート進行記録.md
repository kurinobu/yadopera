# Runbook §1.4（§6.0）品質ゲート — 進行記録

**正本**: `docs/サービス開始までの手順_Runbook.md` **§1.4**  
**実装計画**: `docs/サービス開始までの手順整理_実装計画.md` **§6.0**・**§6.5**  
**大原則**: §6.0 未完了の間は **Stripe live 投入**・**`landing/` 本番反映**・**順7 以降に進まない**（Runbook 冒頭ゲート）。

本ファイルは **「推奨進め方（順番）」** に沿った **チェックリストと証跡置き場**である。完了した項目に日付・実施者を追記する。

**用語（混同防止）**

| 言い方 | 意味 |
|--------|------|
| **Runbook §1.4** | `docs/サービス開始までの手順_Runbook.md` の節「§1.4」＝**品質ゲート一式**（本ファイルのステップ 1〜6 全体）。 |
| **下表の「1.4」** | ステップ 1 の一行目で、**フロント `npm run lint` のみ**を指す。 |

**1.4（lint）**: 2026-03-23、フロントの ESLint 違反を修正し **`npm run lint` が exit 0**（エラー・警告ゼロ）を確認。**変更前スナップショット**: `backups/20260323_lint_fix_pre/`。

---

## ステップ 1 — 証跡の確定（自動テスト・§14.2 相当）

| # | 内容 | 状態 | 証跡・メモ |
|---|------|------|------------|
| 1.1 | **CI 同等** backend pytest（`USE_POSTGRES_TEST=false`, `USE_OPENAI_MOCK=true`） | ✅ 実施済 | `docs/20260323_6.0品質ゲート_実行記録.md` §1 |
| 1.2 | **PostgreSQL** backend pytest（`TEST_DATABASE_URL=@postgres:5432/yadopera_test`） | ✅ 実施済 | 同上 §2（159 passed, 3 skipped） |
| 1.3 | **フロント** `npm run build`（Docker 推奨） | ✅ 実施済 | 同上 §3 |
| 1.4 | **フロント** `npm run lint` | ✅ **実施済・クリーン合格** | 2026-03-23: `cd frontend && npm run lint` → **exit 0**（エラー・警告ゼロ）。証跡: `docs/20260323_6.0品質ゲート_実行記録.md` §4。修正前: **19 errors, 12 warnings**（`backups/20260323_lint_fix_pre/`）。 |
| 1.5 | 実装計画 **§14.2** の B・C（ステージング専用スクリプト等） | ⬜ 要否を判断 | 要る場合は Owner 同伴・URL・シークレットが前提 |

**1.4（lint）手動実行・再検証**（リポジトリルートから）:

```bash
cd frontend && npm run lint
```

Docker 内で揃える場合: `docker compose exec frontend npm run lint`（`frontend/.gitignore` 必須。証跡は `20260323_6.0品質ゲート_実行記録.md` §4 と同じ）。

**基準コミット（リポジトリ記録）**: `develop` @ `0cadf4d`（以降の再実行時はコミットを更新すること）

---

## ステップ 2 — Docker 手動回帰（API / 管理 / ゲスト）

**Runbook §1.4** 該当。**証跡**: 日付・実施者を下表に記入。

| # | 確認項目（最小） | 実施 | 日付 | 実施者 | 結果メモ |
|---|------------------|------|------|--------|----------|
| 2.1 | Backend: `GET /api/v1/health`（または同等） | ✅ | 2026-03-23 | 自動確認＋共有 | **HTTP 200**。レスポンス例: `{"status":"healthy","database":"connected","redis":"connected"}` |
| 2.2 | 管理: ログイン → ダッシュボード表示 | ✅ | 2026-03-23 | 栗原 | `http://localhost:5173/admin/dashboard` で表示確認。ログイン: `test15@air-edison.com`（スクリーンショット共有） |
| 2.3 | 管理: FAQ 一覧・主要導線（1画面以上） | ✅ | 2026-03-23 | 栗原 | 同一セッションでダッシュボード表示・未解決質問一覧・利用状況表示を確認。サイドバーに **FAQ Management** 等の主要導線あり（スクショ） |
| 2.4 | ゲスト: 施設 URL 相当で会話またはトップ表示 | ✅ | 2026-03-23 | 栗原 | `http://localhost:5173/f/33/chat`（静岡テストホテル）。会話引き継ぎコード表示・メッセージ入力欄表示（スクショ） |

（必要に応じて行を増やす。詳細メニューは Runbook **§3** と重なるが、§1.4 は **本番前の品質ゲート**として簡潔でよい。）

### ステップ 2.2〜2.4（あなたがブラウザで実施する場合）

**どこで**: **手元のブラウザ**（ステージングならインターネット経由、ローカル Docker なら localhost）。

| 環境 | 管理ログイン（例） | ゲスト（例） | API health |
|------|-------------------|--------------|------------|
| **ローカル Docker**（`docker compose up`） | `http://localhost:5173/admin/login` | 施設 slug に依存（例: `http://localhost:5173/f/{facility_id}/chat`） | `http://localhost:8000/api/v1/health` |
| **ステージング（Render）** | `https://yadopera-frontend-staging.onrender.com/admin/login` | 例: `https://yadopera-frontend-staging.onrender.com/f/{facility_id}/chat`（総括・Deployment 文書の URL と整合） | `https://yadopera-backend-staging.onrender.com/api/v1/health` |

**手順**: 2.2〜2.4 の表に **日付・実施者（氏名）・「OK」メモ**を書く。ログインには **既存のステージング／ローカル用アカウント**（メール・パスワード）が必要。**シークレットはチャットに貼らない。**  
**証跡画像（ローカル）**: 2026-03-23 共有（管理ダッシュボード・ゲストチャット）。リポジトリ外の場合は社内保管とし、本ファイルの表を正とする。

**シェルでステージングに入る必要は通常ない**。§6.5 も **ブラウザ + Stripe ダッシュボード（test mode）+ 必要なら Render Logs** が主戦場である。

---

## ステップ 3 — 外部連携・データが要るテスト（Owner 共同）

| # | 内容 | 状態 | メモ |
|---|------|------|------|
| 3.1 | Render ステージング API・認証が要る検証 | ✅ 実施済 | 2026-03-23 実行: `curl https://yadopera-backend-staging.onrender.com/api/v1/health` → `{"status":"healthy","database":"connected","redis":"connected"}`、`curl -I https://yadopera-frontend-staging.onrender.com/admin/login` → HTTP 200。`test_staging_facility_available_languages.py` は固定 slug 前提のため 404（データ更新後と不整合）。 |
| 3.2 | ステージング専用スクリプト（§14.2 B） | ✅ 実施済 | 2026-03-23: `test_staging_facility_available_languages_with_users.py` の Standard/Premium ユーザーを `test62` / `test72` に更新し再実行、**5/5 成功**。`verify_phaseE_usage_billing_staging.py` も再実行し **OK=7 / NG=0（All OK）** を確認。秘密値は平文でチャットに残さない。 |

---

## ステップ 4 — ステージング Stripe test mode（§6.5）

実装計画 **§6.5** と同一項目でチェック。**証跡**: 各項目に日付・実施者（ペアなら両名）。

| # | シナリオ | 実施 | 日付 | 備考 |
|---|----------|------|------|------|
| 4.1 | 新規登録（有料）→ Customer/Subscription → 管理画面整合 | ⚠️ 代替実施済 | 2026-03-23 | 代替: `test31@example.com`（Free）で `POST /admin/plans/change` で Mini 化（200）→ `GET /admin/plans` で Mini 反映（200）→ `POST /admin/subscription/cancel`（期間末/即時）両方 200。subscription 操作が成立することを確認。※新規登録 UI 経路は未実施。 |
| 4.2 | プランアップ／ダウン | ✅ 実施済 | 2026-03-23 | `test41@example.com` で `POST /api/v1/admin/plans/change`: Mini→Small→Mini を実行し 200 を確認。 |
| 4.3 | 解約（即時/期間末尾） | ✅ 実施済 | 2026-03-23 | `POST /api/v1/admin/subscription/cancel` を `at_period_end=true` と `false` で実行し 200 を確認。 |
| 4.4 | 請求失敗・カード更新（該当時） | ✅ 実施済 | 2026-03-23 | Stripe サンドボックスで失敗カード `4000 0000 0000 9995` 追加時に「残高不足」エラーを確認（失敗ケース）。その後、請求書作成→確定→顧客に請求で `in_1TE2ppLnkMufdVquepWeDGoE` が `paid` を確認（復旧ケース）。 |
| 4.5 | 請求履歴・領収書・インボイス（設定計画と整合） | ✅ 実施済 | 2026-03-23 | `GET /api/v1/admin/invoices` 200（1件）、`GET /api/v1/admin/invoices/{invoice_id}/receipt` 200（Stripe invoice URL 取得）を確認。 |
| 4.6 | Webhook 失敗→再送→復旧（または手動リコンサイル確認） | ✅ 実施済 | 2026-03-23 | Stripe サンドボックスの Webhook ワークベンチ（送信先 `.../api/v1/webhooks/stripe`）で `invoice.paid` を手動再送し、再送試行が `200 OK` を確認。 |

---

## ステップ 5 — Owner による「全テスト完了」確認

| 項目 | 内容 |
|------|------|
| 記録日 | 2026-03-23 |
| 確認者（Owner） | 栗原 |
| 内容 | ステップ 1〜4 実施済み。4.1 は API 代替で実施し、4.2〜4.6 は証跡確認済み。§1.4 を完了として扱う。 |

---

## ステップ 6 — 本番ブロッカー Issue

| 項目 | 内容 |
|------|------|
| ブロッカー Issue | ✅ ゼロである / ⬜ 許容例外あり |
| 許容例外（ある場合） | 該当なし（4.1 は API 代替で実施済みとして Owner 確認済み）。 |

---

## §1.4 完了の定義（本記録上）

次がすべて満たされたとき、**Runbook §1.4 のチェックを一括で完了**としてよい。

1. ステップ 1 の **1.4（lint）** を **クリーン合格**（または Owner 合意の例外）まで決着  
2. ステップ 2 を **全行実施済**（または「対象外」と理由）  
3. ステップ 3・4 を **計画どおり実施済**（スコープ外は Owner 合意＋記載）  
4. ステップ 5 に **Owner の記録**  
5. ステップ 6 を **ゼロまたは許容例外のみ**

---

### いますぐ実行する次ステップ（固定）

1. **§1.4 完了の共有**: Runbook 正本（`docs/サービス開始までの手順_Runbook.md` §1.4）へ「進行記録 2.1 参照」で完了共有。  
2. **次工程へ進行**: Runbook の当日手順（§2）へ進む前に、対象コミット・実施体制を最終確認。  
3. **必要時の再検証**: Stripe sandbox のイベント再送（4.6）と請求系 API（4.2〜4.5）は同手順で再実行可能。

---

**Document Version**: 2.1  
**Last Updated**: 2026-03-23  
**関連バックアップ**: `backups/20260323_14_principles_workflow/`（初回）、`backups/20260323_step2_followup/`、`backups/20260323_manual_regression_screenshots/`（スクショ反映前）、`backups/20260323_lint_fix_pre/`（lint 修正直前）、`backups/20260323_step3_6_execution/`（ステップ 3〜6 実行前）、`backups/20260323_step3_user_fix/`（ステップ 3 ユーザー修正前）、`backups/20260323_step4_api_execution/`（ステップ 4 API 実行前）、`backups/20260323_step46_complete/`（ステップ 4.6 完了前）
