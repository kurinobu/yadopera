# 多言語実装 実装準備メモ

**作成日**: 2026年2月13日  
**状態**: 文書修正・補足完了。実装準備整備済み。**指示があるまで実装しない。**

---

## 1. 実施済み

- **実装プラン提案書**（`実装プラン提案書_20260207.md`）の修正・補足
  - Phase 4 必須である旨を冒頭・末尾に明記（要約定義書・Phase3_現況と整合）
  - 既存施設は新規登録時のみプリセット適用、embedding は既存フローで生成の補足を §2.2.1 に追加
  - 翻訳手引書の対象言語（英語・繁体中国語）と実装で追加する3言語（zh-TW, fr, ko）の関係を §1.1 に補足
  - **§8 実装準備チェックリスト**を新設（環境・文書・必須ルール・指示後の着手順）

- **評価報告書**（`実装プラン提案書_評価報告_20260213.md`）  
  - 矛盾・抜け・順序・実行可否を評価済み。実装は未実施。

---

## 2. 実装時の中心作業（指示後に実施）

| 項目 | 内容 |
|------|------|
| 修正の中心 | `backend/app/data/faq_presets.py` に 30FAQ × 3言語（zh-TW, fr, ko）= **90 翻訳**を追加 |
| その他 | plan_limits.py / auth_service / facility_service は**変更不要**（動作確認のみ）。フロントも既存のまま。 |
| 環境 | すべて **Docker 環境**で修正・テスト（`docker-compose up -d`） |
| テスト | `test_facility_available_languages.py`、`test_faq_service_language_limit.py` を Docker 内で実行。必要に応じて `test_faq_presets.py`（プラン別フィルタ）を新規作成 |

---

## 3. 参照文書（実装時に見るもの）

- 実装手順・ステップ: `実装プラン提案書_20260207.md` §2.2.3（フェーズA〜D）、§8.3（着手順）
- 評価・前提: `実装プラン提案書_評価報告_20260213.md`
- 計画詳細: `多言語実装計画書（修正版）.md`
- 大原則・フェーズ: `docs/Summary/yadopera-v03-summary.md`、`docs/Phase3/Phase3_現況と残存課題_実装計画_20260209.md`

---

## 4. 指示後の流れ

1. 翻訳データ（90 件）の用意
2. `faq_presets.py` へ zh-TW / fr / ko を追加
3. 構文チェック → Docker 再起動 → 新規登録・言語選択の確認
4. 既存単体テスト実行 → ステージングデプロイ → ブラウザ確認

**現時点**: 実装は行わず、指示を待つ。
