# フェーズC フロントエンド検証メモ

**日時**: 2026年2月13日  
**対象**: 実装プラン提案書 2.2.3 フェーズC（フロントエンド検証）

---

## 1. 言語選択UI確認（LanguageSelect.vue）

| 確認項目 | 結果 | 備考 |
|----------|------|------|
| プラン別言語フィルタ表示 | ✅ コード上確認済み | `response.facility.available_languages` でAPIから取得し、`SUPPORTED_LANGUAGES.filter(lang => availableLanguages.includes(lang.code))` でフィルタ。バックエンドが zh-TW/fr/ko を返せばそのまま表示される。 |
| 利用可能言語が0件のときのフォールバック | ✅ 確認済み | 全言語（SUPPORTED_LANGUAGES）を表示。 |
| エラー時のフォールバック | ✅ 確認済み | 全言語を表示。 |

**対象ファイル**: `frontend/src/views/guest/LanguageSelect.vue`  
**constants.ts**: `SUPPORTED_LANGUAGES` に ja, en, zh-TW, fr, ko が含まれており、表示名・フラグも定義済み。修正不要。

---

## 2. ダークモード確認

| 確認項目 | 結果 | 備考 |
|----------|------|------|
| LanguageSelect.vue | ✅ クラス確認済み | `dark:bg-gray-900`, `dark:text-white`, `dark:text-gray-400`, `dark:bg-red-900/20` 等を使用。 |
| LanguageCard.vue | ✅ クラス確認済み | `dark:bg-gray-800`, `dark:border-gray-700`, `dark:hover:border-blue-600` 等を使用。 |
| ゲスト画面（Welcome, Chat） | ✅ クラス確認済み | 同様に `dark:` 付きクラスで統一。 |

**実機での見映え**: Phase D の統合テストまたはブラウザテストで確認推奨。

---

## 3. レスポンシブ確認

| 確認項目 | 結果 | 備考 |
|----------|------|------|
| LanguageSelect.vue | ✅ クラス確認済み | `min-h-screen`, `max-w-md`, `px-4 py-8`, `space-y-4` でレイアウト。 |
| スマートフォン幅（375px–428px） | 要実機確認 | Phase D または手動でデバイス幅を変更して表示確認。 |

---

## 4. バックアップ・変更まとめ

- **バックアップ**: `backups/20260213_phaseC_multilingual/` に LanguageSelect.vue, constants.ts, LanguageCard.vue を保存。
- **コード変更**: LanguageSelect.vue にコメント1行追加（多言語対応・Phase C 検証済みの記録）。機能変更なし。
- **結論**: フロントエンドは修正不要。API の `available_languages` に zh-TW/fr/ko が含まれればそのまま表示される。Phase D で Docker 起動後のブラウザ確認を推奨。
