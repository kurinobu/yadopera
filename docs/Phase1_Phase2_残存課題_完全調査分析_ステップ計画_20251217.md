# Phase 1・Phase 2: 残存課題 完全調査分析・ステップ計画

**作成日時**: 2025年12月17日（最終更新: 2025年12月20日 11時53分16秒）  
**実施者**: AI Assistant  
**目的**: 残存課題を完全に調査分析し、最も早く解決するステップ計画を立案  
**状態**: ✅ **完全調査分析完了・ステップ計画提示・全ての残存課題完了**

**重要**: 全ての残存課題が完了しました。次のフェーズ（Phase 3: PoC実施）への準備を進めてください。

---

## 1. 残存課題の整理

### 1.1 残存課題一覧（優先度順）

**合計**: **0項目**（全て完了）

#### ✅ 完了した課題（6項目）

1. ✅ **データ不整合の問題** - 完了（ステップ1完了）
   - 会話76と78にUSERメッセージが存在しない
   - サーバーログ: `No user message found for conversation 76`, `No user message found for conversation 78`
   - **解決状況**: 現在のデータベースには存在しないことが確認済み。エラーハンドリングを改善済み。

2. ✅ **ステップ3: FAQ自動学習UIの動作確認** - 完了（ステップ2のブラウザテストで確認完了）
   - 実施内容: FAQ管理画面での未解決質問リスト表示、FAQ改善提案の生成・承認確認
   - **完了日時**: 2025年12月17日 14時41分34秒

3. ✅ **ステップ4: Docker環境での管理画面詳細テスト** - 完了
   - 実施内容: 管理画面の各機能の動作確認
   - **完了日時**: 2025年12月17日 15時45分48秒

4. ✅ **スマートフォンでの画面が真っ白** - 解決済み
   - 影響: スマートフォンユーザーがサービスを利用できない
   - **解決日時**: 2025年12月18日 17時15分00秒
   - **解決方法**: Render.comダッシュボードのHeader設定の削除

5. ✅ **トークンが表示されない** - 解決済み
   - 影響: セッション統合機能が使用できない
   - **解決日時**: 2025年12月18日 18時16分13秒
   - **解決方法**: 統合修正案（修正案1 + 修正案4）を実施

6. ✅ **オフライン時対応** - 実装完了・テスト完了
   - 影響: ユーザー体験の問題（オフライン時に「施設情報の取得に失敗しました」と表示される）
   - **完了日時**: 2025年12月20日 11時48分49秒
   - **実装内容**: `Chat.vue`と`Welcome.vue`でのエラーメッセージ表示ロジックを修正し、オフライン時とネットワークエラー時のメッセージを区別

---

## 2. 各課題の完全調査分析

### 2.1 課題1: データ不整合の問題（🔴 最優先）

#### 2.1.1 問題の詳細

**症状**:
- サーバーログ: `No user message found for conversation 76`, `No user message found for conversation 78`
- 未解決質問リストの取得時に、USERメッセージが見つからない

**発生箇所**:
- `backend/app/services/escalation_service.py`の385行目
- `get_unresolved_questions`メソッド内

**コード確認結果**:
```python
# backend/app/services/escalation_service.py (380-385行目)
else:
    logger.warning(f"No user message found for conversation {conversation.id}")
```

**影響範囲**:
- 未解決質問リストの表示に影響する可能性
- FAQ提案生成時にエラーが発生する可能性（`faq_suggestion_service.py`でも同様のロジックが使用されている）

#### 2.1.2 根本原因の分析

**仮説1: データベースに実際にUSERメッセージが存在しない（可能性: 高い）**
- conversation_id 76と78にUSERロールのメッセージが存在しない
- データ削除やデータ移行の際に削除された可能性
- テストデータ作成時の不具合

**仮説2: メッセージ取得ロジックの問題（可能性: 中）**
- `get_unresolved_questions`メソッドでメッセージを取得する際のフィルタリング条件が厳しすぎる
- `created_at`でソートしているが、順序が正しくない可能性

**仮説3: データベースの整合性の問題（可能性: 低）**
- 外部キー制約の問題
- トランザクションの問題

#### 2.1.3 調査方法

1. **データベース直接確認**:
   ```sql
   -- conversation_id 76と78のメッセージを確認
   SELECT id, role, content, conversation_id, created_at
   FROM messages
   WHERE conversation_id IN (76, 78)
   ORDER BY created_at ASC;
   
   -- conversation_id 76と78の会話情報を確認
   SELECT id, facility_id, created_at
   FROM conversations
   WHERE id IN (76, 78);
   
   -- conversation_id 76と78に関連するエスカレーションを確認
   SELECT id, conversation_id, message_id, created_at
   FROM escalations
   WHERE conversation_id IN (76, 78);
   ```

2. **コードロジックの確認**:
   - `escalation_service.py`の`get_unresolved_questions`メソッドの実装を確認
   - メッセージ取得のフィルタリング条件を確認

3. **サーバーログの確認**:
   - エラーが発生しているタイミングを確認
   - 他の会話でも同様のエラーが発生しているか確認

#### 2.1.4 解決方法

**方法A: データクレンジング（推奨）**
- conversation_id 76と78に関連するデータを削除
- または、USERメッセージを追加（テストデータとして）

**方法B: エラーハンドリングの改善**
- USERメッセージが見つからない場合の処理を改善
- 警告ログを出力するが、エラーとして扱わない

**方法C: データベース整合性チェックの追加**
- 定期的にデータ整合性をチェックするスクリプトを追加
- 不整合が発見された場合に通知

**推奨**: 方法A（データクレンジング）を実施し、その後方法B（エラーハンドリングの改善）を実施

---

### 2.2 課題2: ステップ3: FAQ自動学習UIの動作確認（⚠️ 高優先度）

#### 2.2.1 問題の詳細

**状態**: ⏳ 未実施

**実施内容**:
- FAQ管理画面にアクセス（`/admin/faqs`）
- 「未解決質問リスト」セクションが表示されることを確認
- FAQ改善提案の生成・承認確認

**前提条件**:
- 真っ白画面問題が解決済み（✅ 解決済み）
- 管理画面が正常に表示される（✅ 解決済み）

#### 2.2.2 調査方法

1. **FAQ管理画面へのアクセス確認**:
   - URL: `https://yadopera-frontend-staging.onrender.com/admin/faqs`
   - または: `http://localhost:5173/admin/faqs`（Docker環境）

2. **未解決質問リストの表示確認**:
   - 「未解決質問リスト」セクションが表示されるか確認
   - 未解決質問がリストに表示されるか確認

3. **FAQ改善提案の生成確認**:
   - 「FAQ改善提案」ボタンをクリック
   - 提案が生成されるか確認

4. **FAQ改善提案の承認確認**:
   - 提案を承認してFAQに追加
   - FAQリストに追加されるか確認

#### 2.2.3 解決方法

**実施手順**:
1. Docker環境でFAQ管理画面にアクセス
2. 各機能の動作確認を実施
3. 問題が発見された場合、調査分析を実施（指示がある場合のみ修正）

---

### 2.3 課題3: ステップ4: Docker環境での管理画面詳細テスト（⚠️ 高優先度）

#### 2.3.1 問題の詳細

**状態**: ⏳ 未実施

**実施内容**:
- ログイン機能のテスト
- ダッシュボードのテスト
- FAQ管理画面のテスト
- 施設設定画面のテスト
- FAQ自動学習UIのテスト
- スタッフ不在時間帯対応キューUIのテスト
- QRコード生成UIのテスト
- ゲストフィードバック統計のテスト

#### 2.3.2 調査方法

1. **各機能の動作確認**:
   - Docker環境で各画面にアクセス
   - 各機能の動作を確認
   - 問題が発見された場合、詳細を記録

#### 2.3.3 解決方法

**実施手順**:
1. Docker環境で各機能の動作確認を実施
2. 問題が発見された場合、調査分析を実施（指示がある場合のみ修正）

---

### 2.4 課題4: スマートフォンでの画面が真っ白（⚠️ 高優先度）

#### 2.4.1 問題の詳細

**症状**: スマートフォンで`https://yadopera-frontend-staging.onrender.com/f/test-facility?location=entrance`にアクセスしたが、画面が真っ白で何も表示されない

**影響**: スマートフォンユーザーがサービスを利用できない

**発生場所**: ステージング環境

#### 2.4.2 根本原因の分析

**仮説1: JavaScriptエラー（可能性: 高い）**
- モバイルブラウザでのJavaScript実行エラー
- ポリフィルが不足している
- ES6+の機能がサポートされていない

**仮説2: 静的リソースの読み込みエラー（可能性: 中）**
- JavaScriptファイル（`/assets/index-*.js`）の読み込みに失敗
- CSSファイル（`/assets/index-*.css`）の読み込みに失敗
- ネットワークエラー（タイムアウト、CORSエラー）

**仮説3: Service Workerの登録エラー（可能性: 低）**
- Service Workerの登録に失敗
- キャッシュの問題

#### 2.4.3 調査方法

1. **PCのブラウザでモバイルビューを確認**:
   - Chrome DevToolsのデバイスツールバーを使用
   - モバイルビューで正常に表示されるか確認
   - 正常に表示される場合: スマートフォン固有の問題
   - 正常に表示されない場合: コードの問題

2. **スマートフォンのブラウザで開発者ツールを開く**:
   - Chrome（Android）: USBデバッグを有効化 → PCでChrome DevToolsでリモートデバッグ
   - Safari（iOS）: Safariの開発メニューを有効化 → PCでSafariの開発メニューでリモートデバッグ
   - コンソールエラーを確認
   - ネットワークタブで静的リソースの読み込み状況を確認

#### 2.4.4 解決方法

**調査結果に基づく修正**（指示がある場合のみ）:
- JavaScriptエラーの修正
- モバイルブラウザの互換性問題の修正
- 静的リソースの読み込み問題の修正

---

### 2.5 課題5: トークンが表示されない（⚠️ 高優先度）

#### 2.5.1 問題の詳細

**症状**: 質問を送って回答があってもトークンが表示されない

**影響**: セッション統合機能が使用できない

**発生場所**: ステージング環境

#### 2.5.2 根本原因の分析

**仮説1: トークンが生成されていない（可能性: 高い）**
- セッションIDが取得できていない（`getOrCreateSessionId`が失敗）
- トークン生成APIが呼び出されていない（`sessionApi.generateToken`が呼び出されていない）
- トークン生成APIが失敗している（`POST /api/v1/session/generate`がエラーを返している）

**仮説2: トークンが生成されているが表示されていない（可能性: 中）**
- `sessionToken`が`null`または空文字列（`chatStore.sessionToken`が`null`のまま）
- 表示条件が満たされていない（`SessionTokenDisplay`の`v-if="token"`が`false`）
- コンポーネントがレンダリングされていない

#### 2.5.3 コード確認結果

**フロントエンド実装**:
- `frontend/src/views/guest/Chat.vue`: `sessionToken`は`computed(() => chatStore.sessionToken)`で取得
- `frontend/src/components/guest/SessionTokenDisplay.vue`: `v-if="token"`で表示制御
- `frontend/src/stores/chat.ts`: `sessionToken`は`ref<string | null>(null)`で初期化

**トークン生成のタイミング**:
- `Chat.vue`の`onMounted`で`getOrCreateSessionId`を呼び出し
- セッションIDが取得できたら、既存のトークンを取得（`sessionApi.getTokenBySessionId`）
- トークンがない場合は生成（`sessionApi.generateToken`）
- トークンを`chatStore.setSessionToken`で設定

**確認事項**:
- `Chat.vue`でトークン生成のタイミングが正しく実装されているか確認が必要
- `sessionApi.generateToken`が正しく呼び出されているか確認が必要

#### 2.5.4 調査方法

1. **ブラウザの開発者ツールで確認**:
   - Consoleタブ: `localStorage.getItem('session_id')`でセッションIDを確認
   - Networkタブ: `POST /api/v1/session/generate`が呼び出されているか確認
   - Vue DevTools: `chatStore.sessionToken`の値を確認

2. **コードの確認**:
   - `Chat.vue`の`onMounted`でトークン生成が正しく実装されているか確認
   - `sessionApi.generateToken`が正しく呼び出されているか確認

#### 2.5.5 解決方法

**調査結果に基づく修正**（指示がある場合のみ）:
- トークン生成のタイミングの修正
- トークン表示の条件の修正
- エラーハンドリングの改善

---

### 2.6 課題6: オフライン時対応（⚠️ 高優先度）

#### 2.6.1 問題の詳細

**症状**: オフラインにしリロードすると「施設情報の取得に失敗しました」と表示される

**問題**: ユーザーは「システムエラー」と認識するが、実際には「オフラインです」ということを伝えるべき

**影響**: ユーザー体験の問題

#### 2.6.2 根本原因の分析

**現在の実装**:
- `frontend/src/api/facility.ts`: `getFacility`関数で施設情報を取得
- `frontend/src/views/guest/Chat.vue`: 施設情報取得時にエラーが発生した場合、「施設情報の取得に失敗しました」と表示
- オフライン検出が実装されていない（`navigator.onLine`を使用していない）

**問題点**:
1. オフライン時に「施設情報の取得に失敗しました」と表示される
2. オフライン検出が実装されていない
3. オフライン時の専用メッセージがない

#### 2.6.3 解決方法

**実装が必要な機能**:

1. **オフライン検出の実装**:
   - `composables/useOffline.ts`を作成
   - `navigator.onLine`でオフライン状態を検出
   - `online`/`offline`イベントをリッスン

2. **オフライン時の専用メッセージの表示**:
   - `Chat.vue`で`useOffline`を使用
   - オフライン時に専用メッセージを表示（「オフラインです」「インターネット接続を確認してください」）

3. **エラーメッセージの改善**:
   - オフライン時は「施設情報の取得に失敗しました」ではなく、「オフラインです」と表示
   - ネットワークエラーとオフライン状態を区別

---

## 3. 最も早く解決するステップ計画

### 3.1 ステップ計画の優先順位

**原則**: 
1. **調査が簡単で影響が大きい問題から対応**
2. **依存関係の少ない問題から対応**
3. **実装が簡単で効果が大きい問題から対応**

### 3.2 ステップ計画（実行順序）

#### ステップ1: データ不整合の問題の調査・解決（🔴 最優先）

**目的**: データ不整合の問題を調査し、解決する

**所要時間**: 1-2時間

**実施内容**:

1. **データベース直接確認**（Docker環境）:
   ```bash
   docker-compose exec postgres psql -U postgres -d yadopera
   ```
   ```sql
   -- conversation_id 76と78のメッセージを確認
   SELECT id, role, content, conversation_id, created_at
   FROM messages
   WHERE conversation_id IN (76, 78)
   ORDER BY created_at ASC;
   
   -- conversation_id 76と78の会話情報を確認
   SELECT id, facility_id, created_at
   FROM conversations
   WHERE id IN (76, 78);
   
   -- conversation_id 76と78に関連するエスカレーションを確認
   SELECT id, conversation_id, message_id, created_at
   FROM escalations
   WHERE conversation_id IN (76, 78);
   ```

2. **データクレンジングスクリプトの作成**（指示がある場合のみ）:
   - conversation_id 76と78に関連するデータを削除
   - または、USERメッセージを追加（テストデータとして）

3. **エラーハンドリングの改善**（指示がある場合のみ）:
   - USERメッセージが見つからない場合の処理を改善
   - 警告ログを出力するが、エラーとして扱わない

**完了条件**:
- ✅ データベースの状態を確認
- ✅ データ不整合の原因を特定
- ✅ データクレンジングを実施（指示がある場合のみ）
- ✅ エラーハンドリングを改善（指示がある場合のみ）

**成果物**:
- データ不整合問題の調査分析レポート
- データクレンジングスクリプト（必要に応じて）

---

#### ステップ2: ステップ3: FAQ自動学習UIの動作確認（⚠️ 高優先度）

**目的**: FAQ自動学習UIの動作確認を実施する

**所要時間**: 0.5-1時間

**前提条件**:
- ステップ1完了（データ不整合の問題が解決されている）

**実施内容**:

1. **FAQ管理画面へのアクセス**:
   - URL: `http://localhost:5173/admin/faqs`（Docker環境）
   - または: `https://yadopera-frontend-staging.onrender.com/admin/faqs`（ステージング環境）

2. **未解決質問リストの表示確認**:
   - 「未解決質問リスト」セクションが表示されるか確認
   - 未解決質問がリストに表示されるか確認

3. **FAQ改善提案の生成確認**:
   - 「FAQ改善提案」ボタンをクリック
   - 提案が生成されるか確認

4. **FAQ改善提案の承認確認**:
   - 提案を承認してFAQに追加
   - FAQリストに追加されるか確認

**完了条件**:
- ✅ FAQ管理画面に正常にアクセスできる
- ✅ 未解決質問リストが正常に表示される
- ✅ FAQ改善提案が正常に生成される
- ✅ FAQ改善提案が正常に承認される

**成果物**:
- FAQ自動学習UIの動作確認レポート

---

#### ステップ3: ステップ4: Docker環境での管理画面詳細テスト（⚠️ 高優先度）

**目的**: Docker環境での管理画面の各機能の動作確認を実施する

**所要時間**: 2-3時間

**前提条件**:
- ステップ2完了（FAQ自動学習UIの動作確認完了）

**実施内容**:

1. **ログイン機能のテスト**:
   - ログイン画面にアクセス
   - ログインが正常に動作するか確認

2. **ダッシュボードのテスト**:
   - ダッシュボードが正常に表示されるか確認
   - 各セクションが正常に表示されるか確認

3. **FAQ管理画面のテスト**:
   - FAQ管理画面が正常に表示されるか確認
   - FAQの追加・編集・削除が正常に動作するか確認

4. **施設設定画面のテスト**:
   - 施設設定画面が正常に表示されるか確認
   - 設定の更新が正常に動作するか確認

5. **FAQ自動学習UIのテスト**:
   - 未解決質問リストが正常に表示されるか確認
   - FAQ改善提案の生成・承認が正常に動作するか確認

6. **スタッフ不在時間帯対応キューUIのテスト**:
   - スタッフ不在時間帯対応キューが正常に表示されるか確認
   - 「対応」ボタンが正常に動作するか確認

7. **QRコード生成UIのテスト**:
   - QRコード生成画面が正常に表示されるか確認
   - QRコードの生成・ダウンロードが正常に動作するか確認

8. **ゲストフィードバック統計のテスト**:
   - ゲストフィードバック統計が正常に表示されるか確認
   - グラフが正常に表示されるか確認

**完了条件**:
- ✅ すべての機能が正常に動作する
- ✅ 問題が発見された場合、詳細を記録

**成果物**:
- Docker環境での管理画面詳細テストレポート

---

#### ステップ4: スマートフォンでの画面が真っ白の調査（⚠️ 高優先度）

**目的**: スマートフォンでの画面が真っ白な問題を調査する

**所要時間**: 1-2時間

**前提条件**:
- ステップ3完了（管理画面の動作確認完了）

**実施内容**:

1. **PCのブラウザでモバイルビューを確認**:
   - Chrome DevToolsのデバイスツールバーを使用
   - URL: `https://yadopera-frontend-staging.onrender.com/f/test-facility?location=entrance`
   - モバイルビューで正常に表示されるか確認
   - コンソールエラーを確認
   - ネットワークタブで静的リソースの読み込み状況を確認

2. **スマートフォンのブラウザで開発者ツールを開く**（可能な場合）:
   - Chrome（Android）: USBデバッグを有効化 → PCでChrome DevToolsでリモートデバッグ
   - Safari（iOS）: Safariの開発メニューを有効化 → PCでSafariの開発メニューでリモートデバッグ
   - コンソールエラーを確認
   - ネットワークタブで静的リソースの読み込み状況を確認

3. **調査結果の分析**:
   - エラーの原因を特定
   - 修正方法を検討

**完了条件**:
- ✅ モバイルビューでの表示状況を確認
- ✅ エラーの原因を特定
- ✅ 修正方法を検討

**成果物**:
- スマートフォンでの画面が真っ白な問題の調査分析レポート
- 修正案（指示がある場合のみ）

---

#### ステップ5: トークンが表示されない問題の調査（⚠️ 高優先度）

**目的**: トークンが表示されない問題を調査する

**所要時間**: 1-2時間

**前提条件**:
- ステップ4完了（スマートフォンでの画面が真っ白な問題の調査完了）

**実施内容**:

1. **ブラウザの開発者ツールで確認**:
   - Consoleタブ: `localStorage.getItem('session_id')`でセッションIDを確認
   - Networkタブ: `POST /api/v1/session/generate`が呼び出されているか確認
   - Vue DevTools: `chatStore.sessionToken`の値を確認

2. **コードの確認**:
   - `Chat.vue`の`onMounted`でトークン生成が正しく実装されているか確認
   - `sessionApi.generateToken`が正しく呼び出されているか確認
   - `chatStore.setSessionToken`が正しく呼び出されているか確認

3. **調査結果の分析**:
   - トークンが生成されていない原因を特定
   - または、トークンが表示されない原因を特定
   - 修正方法を検討

**完了条件**:
- ✅ トークン生成のタイミングを確認
   - ✅ セッションIDが取得できているか確認
   - ✅ トークン生成APIが呼び出されているか確認
   - ✅ トークンが`chatStore.setSessionToken`で設定されているか確認
- ✅ トークン表示の条件を確認
   - ✅ `SessionTokenDisplay`の`v-if="token"`が`true`になっているか確認
   - ✅ `chatStore.sessionToken`の値が正しいか確認
- ✅ 問題の原因を特定
- ✅ 修正方法を検討

**成果物**:
- トークンが表示されない問題の調査分析レポート
- 修正案（指示がある場合のみ）

---

#### ステップ6: オフライン時対応の実装（⚠️ 高優先度）

**目的**: オフライン時対応を実装する

**所要時間**: 2-3時間

**前提条件**:
- ステップ5完了（トークンが表示されない問題の調査完了）

**実施内容**:

1. **オフライン検出の実装**:
   - `frontend/src/composables/useOffline.ts`を作成
   - `navigator.onLine`でオフライン状態を検出
   - `online`/`offline`イベントをリッスン

2. **オフライン時の専用メッセージの表示**:
   - `Chat.vue`で`useOffline`を使用
   - オフライン時に専用メッセージを表示（「オフラインです」「インターネット接続を確認してください」）

3. **エラーメッセージの改善**:
   - `frontend/src/api/facility.ts`でオフライン検出を追加
   - オフライン時は「施設情報の取得に失敗しました」ではなく、「オフラインです」と表示
   - ネットワークエラーとオフライン状態を区別

**完了条件**:
- ✅ オフライン検出が実装されている
- ✅ オフライン時の専用メッセージが表示される
- ✅ エラーメッセージが改善されている
- ✅ Docker環境で動作確認が完了している

**成果物**:
- オフライン時対応の実装完了レポート

---

## 4. ステップ計画の実行順序と依存関係

### 4.1 実行順序

1. **ステップ1: データ不整合の問題の調査・解決**（🔴 最優先）
   - 依存関係: なし
   - 所要時間: 1-2時間

2. **ステップ2: ステップ3: FAQ自動学習UIの動作確認**（⚠️ 高優先度）
   - 依存関係: ステップ1完了
   - 所要時間: 0.5-1時間

3. **ステップ3: ステップ4: Docker環境での管理画面詳細テスト**（⚠️ 高優先度）
   - 依存関係: ステップ2完了
   - 所要時間: 2-3時間

4. **ステップ4: スマートフォンでの画面が真っ白の調査**（⚠️ 高優先度）
   - 依存関係: ステップ3完了
   - 所要時間: 1-2時間

5. **ステップ5: トークンが表示されない問題の調査**（⚠️ 高優先度）
   - 依存関係: ステップ4完了
   - 所要時間: 1-2時間

6. **ステップ6: オフライン時対応の実装**（⚠️ 高優先度）✅ **完了**（2025年12月20日 11時48分49秒）
   - 依存関係: ステップ5完了
   - 所要時間: 2-3時間
   - **完了内容**: `Chat.vue`と`Welcome.vue`でのエラーメッセージ表示ロジックを修正し、オフライン時とネットワークエラー時のメッセージを区別。テストも正常動作を確認済み。

### 4.2 総所要時間

**合計**: **8-13時間**（全て完了）

**完了日時**: 2025年12月20日 11時48分49秒

- ステップ1: 1-2時間
- ステップ2: 0.5-1時間
- ステップ3: 2-3時間
- ステップ4: 1-2時間
- ステップ5: 1-2時間
- ステップ6: 2-3時間

---

## 5. 各ステップの詳細実施手順

### 5.1 ステップ1: データ不整合の問題の調査・解決

#### 5.1.1 データベース直接確認

**実施手順**:

1. **Docker環境でPostgreSQLに接続**:
   ```bash
   docker-compose exec postgres psql -U postgres -d yadopera
   ```

2. **conversation_id 76と78のメッセージを確認**:
   ```sql
   SELECT id, role, content, conversation_id, created_at
   FROM messages
   WHERE conversation_id IN (76, 78)
   ORDER BY created_at ASC;
   ```

3. **conversation_id 76と78の会話情報を確認**:
   ```sql
   SELECT id, facility_id, created_at
   FROM conversations
   WHERE id IN (76, 78);
   ```

4. **conversation_id 76と78に関連するエスカレーションを確認**:
   ```sql
   SELECT id, conversation_id, message_id, created_at
   FROM escalations
   WHERE conversation_id IN (76, 78);
   ```

5. **調査結果の分析**:
   - USERメッセージが存在しない場合: データクレンジングを実施（指示がある場合のみ）
   - USERメッセージが存在する場合: コードロジックの問題を調査

#### 5.1.2 データクレンジングスクリプトの作成（指示がある場合のみ）

**実施手順**:

1. **データクレンジングスクリプトの作成**:
   - `backend/cleanup_conversations.py`を作成
   - conversation_id 76と78に関連するデータを削除
   - または、USERメッセージを追加（テストデータとして）

2. **スクリプトの実行**:
   ```bash
   docker-compose exec backend python cleanup_conversations.py
   ```

3. **実行結果の確認**:
   - データが正しく削除されたか確認
   - または、USERメッセージが正しく追加されたか確認

#### 5.1.3 エラーハンドリングの改善（指示がある場合のみ）

**実施手順**:

1. **`escalation_service.py`の修正**:
   - USERメッセージが見つからない場合の処理を改善
   - 警告ログを出力するが、エラーとして扱わない
   - 未解決質問リストから除外する

2. **`faq_suggestion_service.py`の修正**:
   - 同様のエラーハンドリングを追加

3. **動作確認**:
   - Docker環境で動作確認
   - エラーが発生しないことを確認

---

### 5.2 ステップ2: ステップ3: FAQ自動学習UIの動作確認

#### 5.2.1 FAQ管理画面へのアクセス

**実施手順**:

1. **Docker環境でFAQ管理画面にアクセス**:
   - URL: `http://localhost:5173/admin/faqs`
   - ログイン: `test@example.com` / `testpassword123`

2. **画面の表示確認**:
   - FAQ管理画面が正常に表示されるか確認
   - 「未解決質問リスト」セクションが表示されるか確認

#### 5.2.2 未解決質問リストの表示確認

**実施手順**:

1. **未解決質問リストの表示確認**:
   - 「未解決質問リスト」セクションが表示されるか確認
   - 未解決質問がリストに表示されるか確認
   - 各質問の詳細が正しく表示されるか確認

#### 5.2.3 FAQ改善提案の生成確認

**実施手順**:

1. **FAQ改善提案の生成**:
   - 「FAQ改善提案」ボタンをクリック
   - 提案が生成されるか確認
   - 提案の内容が正しいか確認

#### 5.2.4 FAQ改善提案の承認確認

**実施手順**:

1. **FAQ改善提案の承認**:
   - 提案を承認してFAQに追加
   - FAQリストに追加されるか確認
   - 提案がリストから削除されるか確認

---

### 5.3 ステップ3: ステップ4: Docker環境での管理画面詳細テスト

#### 5.3.1 ログイン機能のテスト

**実施手順**:

1. **ログイン画面へのアクセス**:
   - URL: `http://localhost:5173/admin/login`

2. **ログインのテスト**:
   - テストユーザーでログイン: `test@example.com` / `testpassword123`
   - ログインが正常に動作するか確認
   - ダッシュボードにリダイレクトされるか確認

#### 5.3.2 ダッシュボードのテスト

**実施手順**:

1. **ダッシュボードの表示確認**:
   - ダッシュボードが正常に表示されるか確認
   - 各セクションが正常に表示されるか確認

2. **各セクションの動作確認**:
   - スタッフ不在時間帯対応キューが正常に表示されるか確認
   - ゲストフィードバック統計が正常に表示されるか確認
   - グラフが正常に表示されるか確認

#### 5.3.3 FAQ管理画面のテスト

**実施手順**:

1. **FAQ管理画面へのアクセス**:
   - URL: `http://localhost:5173/admin/faqs`

2. **FAQの追加・編集・削除のテスト**:
   - FAQの追加が正常に動作するか確認
   - FAQの編集が正常に動作するか確認
   - FAQの削除が正常に動作するか確認

#### 5.3.4 施設設定画面のテスト

**実施手順**:

1. **施設設定画面へのアクセス**:
   - URL: `http://localhost:5173/admin/settings`

2. **設定の更新のテスト**:
   - 施設設定画面が正常に表示されるか確認
   - 設定の更新が正常に動作するか確認

#### 5.3.5 その他の機能のテスト

**実施手順**:

1. **FAQ自動学習UIのテスト**:
   - 未解決質問リストが正常に表示されるか確認
   - FAQ改善提案の生成・承認が正常に動作するか確認

2. **スタッフ不在時間帯対応キューUIのテスト**:
   - スタッフ不在時間帯対応キューが正常に表示されるか確認
   - 「対応」ボタンが正常に動作するか確認

3. **QRコード生成UIのテスト**:
   - QRコード生成画面が正常に表示されるか確認
   - QRコードの生成・ダウンロードが正常に動作するか確認

4. **ゲストフィードバック統計のテスト**:
   - ゲストフィードバック統計が正常に表示されるか確認
   - グラフが正常に表示されるか確認

---

### 5.4 ステップ4: スマートフォンでの画面が真っ白の調査

#### 5.4.1 PCのブラウザでモバイルビューを確認

**実施手順**:

1. **Chrome DevToolsのデバイスツールバーを使用**:
   - Chromeで`https://yadopera-frontend-staging.onrender.com/f/test-facility?location=entrance`にアクセス
   - F12で開発者ツールを開く
   - デバイスツールバーを有効化（Ctrl+Shift+M）
   - モバイルデバイスを選択（iPhone、Androidなど）

2. **モバイルビューでの表示確認**:
   - 画面が正常に表示されるか確認
   - コンソールエラーを確認
   - ネットワークタブで静的リソースの読み込み状況を確認

3. **調査結果の分析**:
   - 正常に表示される場合: スマートフォン固有の問題
   - 正常に表示されない場合: コードの問題

#### 5.4.2 スマートフォンのブラウザで開発者ツールを開く（可能な場合）

**実施手順**:

1. **Chrome（Android）でのリモートデバッグ**:
   - Android端末でUSBデバッグを有効化
   - PCでChrome DevToolsでリモートデバッグ
   - コンソールエラーを確認
   - ネットワークタブで静的リソースの読み込み状況を確認

2. **Safari（iOS）でのリモートデバッグ**:
   - iOS端末でSafariの開発メニューを有効化
   - PCでSafariの開発メニューでリモートデバッグ
   - コンソールエラーを確認
   - ネットワークタブで静的リソースの読み込み状況を確認

#### 5.4.3 調査結果の分析

**実施手順**:

1. **エラーの原因を特定**:
   - JavaScriptエラーの場合: エラーメッセージを確認
   - 静的リソースの読み込みエラーの場合: ネットワークタブで確認
   - Service Workerの登録エラーの場合: Applicationタブで確認

2. **修正方法を検討**:
   - エラーの原因に基づいて修正方法を検討
   - 修正案を提示（指示がある場合のみ修正）

---

### 5.5 ステップ5: トークンが表示されない問題の調査

#### 5.5.1 ブラウザの開発者ツールで確認

**実施手順**:

1. **ConsoleタブでセッションIDを確認**:
   ```javascript
   localStorage.getItem('session_id')
   ```
   - セッションIDが取得できているか確認

2. **Networkタブでトークン生成APIを確認**:
   - `POST /api/v1/session/generate`が呼び出されているか確認
   - レスポンスにトークンが含まれているか確認
   - エラーレスポンスがないか確認

3. **Vue DevToolsで`chatStore.sessionToken`を確認**:
   - `chatStore.sessionToken`の値を確認
   - `SessionTokenDisplay`コンポーネントの`props`を確認

#### 5.5.2 コードの確認

**実施手順**:

1. **`Chat.vue`の`onMounted`でトークン生成が正しく実装されているか確認**:
   - `getOrCreateSessionId`が呼び出されているか確認
   - `sessionApi.getTokenBySessionId`が呼び出されているか確認
   - `sessionApi.generateToken`が呼び出されているか確認
   - `chatStore.setSessionToken`が呼び出されているか確認

2. **`sessionApi.generateToken`の実装を確認**:
   - APIエンドポイントが正しく実装されているか確認
   - エラーハンドリングが正しく実装されているか確認

#### 5.5.3 調査結果の分析

**実施手順**:

1. **トークンが生成されていない原因を特定**:
   - セッションIDが取得できていない場合: `getOrCreateSessionId`の問題
   - トークン生成APIが呼び出されていない場合: `Chat.vue`の実装の問題
   - トークン生成APIが失敗している場合: バックエンドの問題

2. **トークンが表示されない原因を特定**:
   - `sessionToken`が`null`の場合: `setSessionToken`が呼び出されていない
   - 表示条件が満たされていない場合: `v-if="token"`の問題

3. **修正方法を検討**:
   - 原因に基づいて修正方法を検討
   - 修正案を提示（指示がある場合のみ修正）

---

### 5.6 ステップ6: オフライン時対応の実装

#### 5.6.1 オフライン検出の実装

**実施手順**:

1. **`frontend/src/composables/useOffline.ts`を作成**:
   ```typescript
   import { ref, onMounted, onUnmounted } from 'vue'
   
   export function useOffline() {
     const isOffline = ref(!navigator.onLine)
     
     const handleOnline = () => {
       isOffline.value = false
     }
     
     const handleOffline = () => {
       isOffline.value = true
     }
     
     onMounted(() => {
       window.addEventListener('online', handleOnline)
       window.addEventListener('offline', handleOffline)
     })
     
     onUnmounted(() => {
       window.removeEventListener('online', handleOnline)
       window.removeEventListener('offline', handleOffline)
     })
     
     return { isOffline }
   }
   ```

2. **動作確認**:
   - Docker環境で動作確認
   - オフライン検出が正常に動作するか確認

#### 5.6.2 オフライン時の専用メッセージの表示

**実施手順**:

1. **`Chat.vue`で`useOffline`を使用**:
   ```typescript
   import { useOffline } from '@/composables/useOffline'
   
   const { isOffline } = useOffline()
   ```

2. **オフライン時の専用メッセージを表示**:
   ```vue
   <div v-if="isOffline" class="bg-yellow-50 dark:bg-yellow-900/20 border border-yellow-200 dark:border-yellow-800 rounded-lg px-4 py-3 mb-4">
     <p class="text-sm text-yellow-800 dark:text-yellow-200">
       オフラインです / Offline
     </p>
     <p class="text-xs text-yellow-600 dark:text-yellow-400 mt-1">
       インターネット接続を確認してください / Please check your internet connection
     </p>
   </div>
   ```

3. **動作確認**:
   - Docker環境で動作確認
   - オフライン時に専用メッセージが表示されるか確認

#### 5.6.3 エラーメッセージの改善

**実施手順**:

1. **`frontend/src/api/facility.ts`でオフライン検出を追加**:
   ```typescript
   import { useOffline } from '@/composables/useOffline'
   
   const { isOffline } = useOffline()
   
   async getFacility(slug: string, location?: string): Promise<FacilityPublicResponse> {
     if (isOffline.value) {
       throw new Error('オフラインです / Offline')
     }
     // 既存の実装
   }
   ```

2. **`Chat.vue`でエラーメッセージを改善**:
   ```typescript
   catch (err: any) {
     if (isOffline.value) {
       error.value = 'オフラインです / Offline'
     } else {
       error.value = err.message || '施設情報の取得に失敗しました'
     }
   }
   ```

3. **動作確認**:
   - Docker環境で動作確認
   - オフライン時に「オフラインです」と表示されるか確認
   - オンライン時に「施設情報の取得に失敗しました」と表示されるか確認

---

## 6. ステップ計画の実行チェックリスト

### 6.1 ステップ1: データ不整合の問題の調査・解決

- [ ] データベース直接確認を実施
- [ ] conversation_id 76と78のメッセージを確認
- [ ] conversation_id 76と78の会話情報を確認
- [ ] conversation_id 76と78に関連するエスカレーションを確認
- [ ] 調査結果を分析
- [ ] データクレンジングスクリプトを作成（指示がある場合のみ）
- [ ] エラーハンドリングを改善（指示がある場合のみ）

### 6.2 ステップ2: ステップ3: FAQ自動学習UIの動作確認

- [ ] FAQ管理画面にアクセス
- [ ] 未解決質問リストの表示確認
- [ ] FAQ改善提案の生成確認
- [ ] FAQ改善提案の承認確認

### 6.3 ステップ3: ステップ4: Docker環境での管理画面詳細テスト

- [ ] ログイン機能のテスト
- [ ] ダッシュボードのテスト
- [ ] FAQ管理画面のテスト
- [ ] 施設設定画面のテスト
- [ ] FAQ自動学習UIのテスト
- [ ] スタッフ不在時間帯対応キューUIのテスト
- [ ] QRコード生成UIのテスト
- [ ] ゲストフィードバック統計のテスト

### 6.4 ステップ4: スマートフォンでの画面が真っ白の調査

- [ ] PCのブラウザでモバイルビューを確認
- [ ] スマートフォンのブラウザで開発者ツールを開く（可能な場合）
- [ ] 調査結果を分析
- [ ] 修正方法を検討（指示がある場合のみ）

### 6.5 ステップ5: トークンが表示されない問題の調査

- [ ] ブラウザの開発者ツールで確認
- [ ] コードの確認
- [ ] 調査結果を分析
- [ ] 修正方法を検討（指示がある場合のみ）

### 6.6 ステップ6: オフライン時対応の実装

- [ ] オフライン検出の実装
- [ ] オフライン時の専用メッセージの表示
- [ ] エラーメッセージの改善
- [ ] 動作確認

---

## 7. 大原則準拠の確認

### 7.1 根本解決 > 暫定解決

**評価**: ✅ **根本解決を優先**

**理由**:
- データ不整合の問題: データクレンジングとエラーハンドリングの改善で根本解決
- スマートフォンでの画面が真っ白: 原因を特定して根本解決
- トークンが表示されない: 原因を特定して根本解決
- オフライン時対応: オフライン検出の実装で根本解決

### 7.2 シンプル構造 > 複雑構造

**評価**: ✅ **シンプル構造を優先**

**理由**:
- 各ステップは独立して実施可能
- 複雑な依存関係がない
- シンプルな調査・修正手順

### 7.3 統一・同一化 > 特殊独自

**評価**: ✅ **統一・同一化を優先**

**理由**:
- 既存のコードパターンに従う
- 既存のエラーハンドリングパターンに従う
- 環境ごとに異なる設定を使用しない

### 7.4 具体的 > 一般

**評価**: ✅ **具体的な手順を提示**

**理由**:
- 各ステップの具体的な実施手順を明確化
- 具体的なSQLクエリ、コード例を提示
- 具体的な確認項目を提示

### 7.5 拙速 < 安全確実

**評価**: ✅ **安全確実を優先**

**理由**:
- 各ステップで十分な調査・確認を実施
- Docker環境で動作確認を実施
- 修正は指示がある場合のみ実施

### 7.6 Docker環境必須

**評価**: ✅ **Docker環境必須**

**理由**:
- すべての調査・テストはDocker環境で実施
- ローカル環境で直接実行しない

---

## 8. まとめ

### 8.1 残存課題の整理

**合計**: **6項目**
- 🔴 最優先: 1項目（データ不整合: 会話76と78にUSERメッセージが存在しない）
- ⚠️ 高優先度: 5項目（ステップ3、ステップ4、スマートフォンでの画面が真っ白、トークンが表示されない、オフライン時対応）

### 8.2 ステップ計画の概要

**実行順序**:
1. ステップ1: データ不整合の問題の調査・解決（1-2時間）
2. ステップ2: ステップ3: FAQ自動学習UIの動作確認（0.5-1時間）
3. ステップ3: ステップ4: Docker環境での管理画面詳細テスト（2-3時間）
4. ステップ4: スマートフォンでの画面が真っ白の調査（1-2時間）
5. ステップ5: トークンが表示されない問題の調査（1-2時間）
6. ステップ6: オフライン時対応の実装（2-3時間）

**総所要時間**: **8-13時間**

### 8.3 次のステップ

**最優先**: ステップ1（データ不整合の問題の調査・解決）から開始

**重要**: 
1. 指示があるまで修正を実施しない
2. 調査分析とステップ計画の提示のみ
3. 各ステップで十分な調査・確認を実施

---

**完全調査分析・ステップ計画提示完了日時**: 2025年12月17日  
**最終更新日時**: 2025年12月20日 11時53分16秒  
**状態**: ✅ **完全調査分析完了・ステップ計画提示・全ての残存課題完了**

**重要**: 全ての残存課題が完了しました。次のフェーズ（Phase 3: PoC実施）への準備を進めてください。
