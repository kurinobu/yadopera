# ロールバック先 完全調査報告（このセッションが始まった場所まで）

**調査日**: 2026年3月13日  
**目的**: 「このセッションが始まった場所までロールバックする」ために、**どこのどの状態に戻すか**を完全に特定する。  
**指示**: 報告のみ。実行は指示があるまで行わない。

---

## 1. 結論：ロールバック先

| 項目 | 内容 |
|------|------|
| **ロールバック先** | **コミット `fdd69d3`** の作業ツリーに完全に一致させる |
| **コミット概要** | `feat: プラン超過時の挙動（管理者選択制）実装・ダッシュボード文言修正・ヘルプFAQ追加` |
| **コミット日時** | 2026-03-13 12:35:56 +0900 |
| **フルハッシュ** | `fdd69d35b8b9da1f5a92809097a224284a1332c5` |

**理由**: 現在のブランチ `feature/csv-manual-paid-option-float` の HEAD はこのコミット。CSV一括登録・代行申し込み（Step 0〜3）の作業は**一切コミットされておらず**、すべて「未コミットの変更」または「未追跡ファイル」として存在している。したがって「このセッションが始まった場所」＝ブランチが指していた**最後のコミットの状態**＝**fdd69d3 のクリーンな作業ツリー**に戻すことで、CSV 関連の無駄な作業をすべて取り除く。

---

## 2. 現在の状態（ロールバック前）

- **ブランチ**: `feature/csv-manual-paid-option-float`
- **HEAD**: `fdd69d3`（= `develop` と同じ）

### 2.1 追跡済みで変更されているファイル（fdd69d3 と差分があるもの）

ロールバック時に **`fdd69d3` の内容に復元**する対象。

| ファイル | 備考 |
|----------|------|
| `backend/scripts/insert_operator_faqs.py` | Step 3 関連（faq_bulk_import_paid_option 追加等） |
| `docs/Architecture/やどぺら_v0.3_アーキテクチャ設計書.md` | その他変更 |
| `docs/Phase3/Phase3_現況と残存課題_実装計画_20260209.md` | その他変更 |
| `docs/Summary/yadopera-v03-summary.md` | その他変更 |
| `docs/help_system_faq_data.md` | Step 3 関連（CSV代行FAQ追加） |
| `docs/プロジェクト現況と今後の計画_総括.md` | その他変更 |
| `docs/エンドユーザー_プロジェクト現況FAQ追加_修正例.md` | その他変更 |
| `docs/施設管理者向けエンドユーザーFAQ_文言修正と運用.md` | その他変更 |
| `frontend/src/components/admin/Sidebar.vue` | Step 3（CSV代行の申し込みリンク追加） |
| `frontend/src/components/help/HelpModal.vue` | Step 3（CSV代行申し込みリンク追加） |
| `frontend/src/router/admin.ts` | Step 3（/admin/csv-bulk-request ルート追加） |
| `frontend/src/views/admin/FaqManagement.vue` | Step 3（代行をご希望の方はこちらリンク追加） |
| `frontend/src/views/admin/Manual.vue` | Step 1・3 関連（4.8 詳細化・有料代行案内追加） |

※ `docs/CSV一括登録_マニュアル・有料オプション・フロート申し込み_実装計画.md` は、fdd69d3 のコミットには**含まれていない**（別コミットで追加されたか、未追跡で追加されたドキュメント）。現状は「変更あり」として表示されているため、リポジトリ上では追跡されている可能性あり。ロールバック時は **fdd69d3 に存在しなければ削除**、存在すれば **fdd69d3 の内容に復元**。

### 2.2 未追跡ファイル（ロールバックで削除する対象）

「このセッションが始まった場所」＝ fdd69d3 の状態には**存在しない**ため、ロールバック時に**削除**する。

#### CSV 代行・Step 0〜3 で追加されたと判断できるもの

| ファイル/ディレクトリ | 備考 |
|------------------------|------|
| `frontend/src/views/admin/CsvBulkRequest.vue` | Step 3 で追加。Formspree 申し込みページ。 |
| `docs/20260313_CSV一括登録_代行申し込み_FormspreeからAPI_Brevoへの修正_実行計画.md` | 本セッションで作成（日付付きにリネーム済み）。 |
| `backend/scripts/README.md` | Step 2 で追加。 |
| `backend/scripts/bulk_upload_faq_csv_for_facility.py` | Step 2 で追加。代行履行用スクリプト。 |
| `docs/CSV_bulk_registration_function_implementation_plan/CSV一括登録_有料オプション_運用開始チェックリスト.md` | Step 2 で追加。 |
| `docs/CSV_bulk_registration_function_implementation_plan/files/FAQ_CSV_sample_for_script_test.csv` | テスト用等。 |
| `test/` | テスト用CSV等（例: csv_bulk_test.csv）。 |

#### その他の未追跡（CSV と無関係のバックアップ・他機能のドキュメント）

- `backups/` 配下一式（20260309〜20260313 等）
- `docs/20260307_...` 等の他ドキュメント
- `frontend/public/pwa-*.png.bak_*`

**ロールバック方針**: 「このセッションが始まった場所」に厳密に戻すなら、**CSV 関連の未追跡のみ削除**し、backups 等は**触れない**（残す）とするのが安全。必要なら「CSV 関連未追跡のみ削除」と「backups を含む未追跡をすべて削除」のどちらにするか指示で指定する。

---

## 3. ロールバックの実行手順（指示があった場合に実施する内容）

### 3.1 追跡済みファイルを fdd69d3 の内容に戻す

```bash
cd /Users/kurinobu/projects/yadopera
git restore --source=fdd69d3 -- \
  backend/scripts/insert_operator_faqs.py \
  "docs/Architecture/やどぺら_v0.3_アーキテクチャ設計書.md" \
  "docs/Phase3/Phase3_現況と残存課題_実装計画_20260209.md" \
  docs/Summary/yadopera-v03-summary.md \
  docs/help_system_faq_data.md \
  "docs/プロジェクト現況と今後の計画_総括.md" \
  "docs/エンドユーザー_プロジェクト現況FAQ追加_修正例.md" \
  "docs/施設管理者向けエンドユーザーFAQ_文言修正と運用.md" \
  frontend/src/components/admin/Sidebar.vue \
  frontend/src/components/help/HelpModal.vue \
  frontend/src/router/admin.ts \
  frontend/src/views/admin/FaqManagement.vue \
  frontend/src/views/admin/Manual.vue
```

※ `docs/CSV一括登録_マニュアル・有料オプション・フロート申し込み_実装計画.md` が追跡されている場合のみ、同じく `git restore --source=fdd69d3 -- "docs/CSV一括登録_マニュアル・有料オプション・フロート申し込み_実装計画.md"` を実行。fdd69d3 に無いファイルなら `git restore` では消えないため、別途 `git rm --cached` またはファイル削除で対応。

### 3.2 未追跡の CSV 関連を削除する

```bash
cd /Users/kurinobu/projects/yadopera
rm -f frontend/src/views/admin/CsvBulkRequest.vue
rm -f "docs/20260313_CSV一括登録_代行申し込み_FormspreeからAPI_Brevoへの修正_実行計画.md"
rm -f "docs/20260313_CSV一括登録_ロールバック先_調査報告.md"
rm -f "docs/20260313_CSV一括登録_代行申し込み_Formspree選定事故_原因と再発防止策.md"
rm -f backend/scripts/README.md
rm -f backend/scripts/bulk_upload_faq_csv_for_facility.py
rm -f "docs/CSV_bulk_registration_function_implementation_plan/CSV一括登録_有料オプション_運用開始チェックリスト.md"
rm -f "docs/CSV_bulk_registration_function_implementation_plan/files/FAQ_CSV_sample_for_script_test.csv"
rm -rf test/
```

※ **実装計画書** `docs/CSV一括登録_マニュアル・有料オプション・フロート申し込み_実装計画.md` は **fdd69d3 に含まれていない**。厳密に fdd69d3 の状態に戻す場合は削除する:  
`rm -f "docs/CSV一括登録_マニュアル・有料オプション・フロート申し込み_実装計画.md"`  
（追跡済みの場合は `git restore --source=fdd69d3` では消えないため、上記 rm で作業ツリーから削除。必要なら `git checkout -- "docs/CSV一括登録_マニュアル・有料オプション・フロート申し込み_実装計画.md"` でインデックス側の変更も破棄するか、ファイル削除後に `git status` で確認。）

※ 日付付きドキュメント（20260313_*）は**ロールバック後も残す**場合は、上記 rm の該当 3 行を削除すること。**残さない**（fdd69d3 に無いので消す）場合はそのまま実行。

### 3.3 ロールバック後の確認

```bash
git status
```

- 追跡ファイルはすべて fdd69d3 と一致（変更なし）。
- CsvBulkRequest.vue・実行計画・Step 2 で追加した scripts/docs・test は存在しない。

---

## 4. ロールバック後に残る状態

- **ブランチ**: `feature/csv-manual-paid-option-float` のまま（変更なし）。
- **HEAD**: `fdd69d3` のまま。
- **作業ツリー**: fdd69d3 の内容と一致（CSV 一括・代行申し込みに関する変更は一切なし）。
- **未追跡**: backups/ や他機能の未追跡ドキュメント等は、3.2 で削除しなければそのまま残る。

---

## 5. まとめ

| 質問 | 回答 |
|------|------|
| **どこにロールバックするか** | **コミット `fdd69d3`** の作業ツリーに一致させる。 |
| **どのように戻すか** | (1) 上記 13 ファイル（＋実装計画書が追跡されていればそれ）を `git restore --source=fdd69d3` で復元。(2) CSV 関連の未追跡ファイル（CsvBulkRequest.vue、実行計画、Step 2 の scripts/docs、test）を削除。 |
| **実行するか** | **指示があるまで実行しない。** 本報告は調査結果の報告のみ。 |

以上。
