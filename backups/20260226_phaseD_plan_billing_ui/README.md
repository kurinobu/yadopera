# Phase D: プラン・請求 UI 実装前バックアップ

**日付**: 2026-02-26  
**内容**: 管理画面「プラン・請求」ページ追加に伴う変更前のルート・サイドバー参照用。

- `admin_routes_before_phaseD.ts` … `frontend/src/router/admin.ts` の Phase D 追加前コピー
- Sidebar は `frontend/src/components/admin/Sidebar.vue` を直接編集するため、同一ディレクトリにはコピーしていない（git で復元可能）

**復元**: ルートのみ復元する場合は `admin_routes_before_phaseD.ts` を `frontend/src/router/admin.ts` に上書き。
